# FIXLOG — AI Story Book 출시 전 결함 수정 구현 (2026-07-20)

> 수임: 구현 전담 개발자 / 발주: CTO. 정본: `docs/DEV_SPEC_2026-07-20.md` v1.3.
> 이 파일이 진행 상태의 정본(요약 드리프트 금지 — FIXLOG·git log로 재동기화).
> 각 티켓: ID / 변경 파일 / 근본원인 / 추가 테스트명 / red→green 확인 / 게이트 결과 / 플래그(FALSE-GREEN-RISK·NEEDS-DECISION·이탈).

## 베이스라인 (2026-07-21 실측)
- 백엔드 pytest: **434 pass / 1 known-fail** (`test_shared_openapi_contract_is_committed_and_current` — 로컬 `apps/api/.env` `APP_VERSION=0.2.0` 잔재, info.version 드리프트). ruff pass.
- 모바일 flutter (3.38.7): 194 pass(문서값, W1 착수 시 재측정).
- 단일 alembic head: `d0e1f2a3b4c5`.
- 게이트 판정 기준: **"베이스라인 대비 회귀 0"** (전체 그린 아님). W1에서 M8-backend로 known-fail 그린화 후 이후 웨이브는 완전 그린 기대.
- 환경: venv Python 3.9.6, ruff 0.14.14, flutter 3.38.7, docker(PG 사용가능), alembic OK.

---

## Wave 1 — 치명 3 + M8 베이스라인 그린화

### M8 (백엔드 부분만, W1 승격) — app_version 코드 정본화
- **변경 파일**: `apps/api/src/core/config.py` (app_version: pydantic 필드 → 모듈 상수 `APP_VERSION` + `@property`), `apps/api/tests/test_app_version.py` (신규).
- **근본원인**: `app_version`이 pydantic Settings 필드라 `APP_VERSION` env(로컬 `.env`의 0.2.0 잔재)로 오버라이드 → 런타임 info.version 드리프트 → 계약 신선도 테스트 known-fail.
- **추가 테스트**: `test_app_version_not_overridable_by_env` — APP_VERSION=9.9.9 설정 후에도 app_version=='1.0.0'.
- **red→green**: RED `'9.9.9' == '1.0.0'` AssertionError → property 적용 후 GREEN. 베이스라인 known-fail(`test_shared_openapi_contract_is_committed_and_current`)도 동시 그린화.
- **게이트**: pytest test_app_version + test_openapi_contract 5 pass · ruff pass.
- **범위 메모**: M8의 모바일(package_info_plus)·CI/e2e(APP_VERSION 1.0.0) 부분은 §B4대로 **W6에 잔류**(W1은 베이스라인 그린화용 백엔드 정정만).

### M17 — subscriptions active 부분 유니크 + 동시 create 흡수
- **변경 파일**: `apps/api/src/models/db.py` (Subscription `__table_args__` 부분 유니크 `uq_subscriptions_active_per_user`), `apps/api/src/services/credits.py` (create_subscription: 취소 UPDATE 선-flush 순서 보장 + IntegrityError 2회 재시도 흡수), `apps/api/alembic/versions/e5f6a7b8c9d0_*.py` (신규: 기존 다중 active dedup 후 인덱스), `apps/api/tests/test_subscription_uniqueness.py` (신규).
- **근본원인**: `get_active_subscription` SELECT 후 active INSERT 사이에 DB 제약 부재 → 동시 두 호출이 active 2행 생성 → periodic_credits 영구 이중 지급.
- **추가 테스트**: `test_active_subscription_partial_unique_blocks_second`, `test_create_subscription_happy_upgrade_keeps_single_active`, `test_create_subscription_absorbs_race_integrity_error`.
- **red→green**: RED(부분유니크 미존재로 2행 허용 / 경쟁 시 2 active) → 제약+재시도 후 GREEN. 3 신규 + 기존 subscription/payment 29 pass.
- **플러시 순서 주의**: 새 제약 하에서 정상 업그레이드(basic→premium)도 취소 UPDATE를 새 active INSERT보다 먼저 flush하지 않으면 위반 → 취소 직후 `db.flush()` 삽입(happy-path 회귀 방지).
- **money 마이그레이션 실DB(PG) 리허설**: ephemeral postgres:16, 이전 head(d0e1f2a3b4c5)에서 u1 active 2행+u2 1행 시드 → `alembic upgrade head` → **dedup 결과 u1=1(premium=최신 유지)·cancelled 1·u2=1**, 2nd active insert는 `duplicate key ... uq_subscriptions_active_per_user`로 차단, cancelled 공존 OK. ✅
- **⚠ 사전 존재 드리프트(NEEDS-DECISION 아님, 스코핑 메모)**: `alembic check`가 `books.retelling_source_book_id` FK 미기록 드리프트 1건 보고 — **M17과 무관한 사전 존재분(= W5 M10 대상)**. M17 자체 마이그레이션은 diff 0(subscriptions 관련 diff 없음). money-migration 공통 DoD의 `alembic check diff 0`는 M10 완료 시 충족 예정.
- **게이트**: pytest 통과 · ruff 통과 · alembic heads 단일(e5f6a7b8c9d0) · SQLite+PG upgrade OK.

### C2 — 파이프라인 인-플라이트 최종 실패 시 크레딧 환불 배선
- **변경 파일**: `apps/api/src/services/orchestrator.py` (mark_job_failed), `apps/api/src/services/tasks.py` (_mark_job_failed_async), `apps/api/tests/test_payment_integrity.py` (신규 4테스트).
- **근본원인**: 선차감(reference_id=job_id) 후 정상 실패 경로(orchestrator.mark_job_failed / celery _mark_job_failed_async)가 refund_for_job을 호출하지 않고 job_monitor 스캔은 이미 failed인 잡을 제외 → 유료 크레딧 영구 소실.
- **핸드오프 정정 MA3 적용(원 fix보다 우선)**: refund_for_job(commit=False)이 add_credits의 무조건 rollback을 타면 미커밋 job.status='failed'까지 삼켜 DoD 불가 → **(a) 실패 status 먼저 commit → 별도 트랜잭션에서 refund_for_job(commit=True)**. user_key는 commit 전 캡처. 환불은 try/except로 감싸 실패가 잡 마킹을 막지 않고 logger.warning만.
- **G3 결정 반영**: 전 실패 코드 환불(관대) — SAFETY_INPUT 예외 분기 미추가.
- **추가 테스트**: `test_orchestrator_mark_job_failed_refunds_charged_job`, `test_mark_job_failed_refund_idempotent`, `test_tasks_mark_job_failed_async_refunds`, `test_mark_job_failed_persists_status_even_if_refund_fails`(MA3 DoD).
- **red→green**: RED(3 core: 잔액 2 유지=미환불) → 배선 후 GREEN. MA3 가드 테스트는 환불 강제실패 시 status='failed' 영속·잔액 2 확인.
- **게이트**: pytest test_payment_integrity 8 pass · ruff pass.
- **폭발 반경 메모**: mark_job_failed는 W3 H10(fence)·M23(done-fence) 공유 — C2의 refund 삽입을 먼저 착지(§3.2)했으니 H10이 그 위에 조건부 UPDATE fence를 얹을 것.

### C3 — IAP 검증 실패 시 finally completePurchase 무조건 호출(소모성 대금 유실)
- **변경 파일**: `apps/mobile/lib/services/iap_service.dart` (buyProduct autoConsume:false + finishPurchase 플랫폼 라우팅 + androidConsume 시임), `apps/mobile/lib/screens/credits_screen.dart` (_handlePurchaseUpdate 재구성), `apps/mobile/pubspec.yaml`(+in_app_purchase_android ^0.4.0)·`pubspec.lock`, `apps/mobile/test/iap_purchase_flow_test.dart`(신규 7테스트).
- **근본원인**: `_handlePurchaseUpdate`의 finally가 verifyIap 성공 여부와 무관하게 completePurchase 호출 → 검증 실패(순단·5xx) 시 트랜잭션 finish/consume·크레딧 미지급·구제경로 전무.
- **수정**: finally에서 complete 제거 → verify 성공 직후에만 `finishPurchase`. catch 시 미완료(pending 유지)로 다음 실행 재검증. buyConsumable autoConsume:false.
- **핸드오프 정정 B1 적용(원 fix보다 우선)**: autoConsume:false + completePurchase만으로는 Android에서 consume이 영영 안 돼 재구매 ITEM_ALREADY_OWNED 영구 실패 → **verify 성공 시 Android는 `InAppPurchaseAndroidPlatformAddition.consumePurchase` 명시 호출**(iOS는 completePurchase). 테스트 시임 `androidConsume`로 라우팅 검증.
- **핸드오프 정정 MI2 적용**: 723행 in-flight 중복 이벤트 가드는 complete 호출 금지 → drop만.
- **l10n**: 신규 문자열 없음 — 기존 `creditsVerifyFailed`('잠시 후 다시 시도' 취지) 재사용. gen-l10n 불필요.
- **추가 테스트**: buyProduct autoConsume:false / buyNonConsumable / finishPurchase 3-라우팅(iOS complete·Android consume·Android non-consumable complete) / 화면 제어흐름 verify 실패→미완료·성공→완료 1회.
- **red→green**: 화면 버그 RED를 `git stash push credits_screen.dart`로 원 버그 복원 후 실증 — `verifyIap failure` 테스트가 `Expected:<0> Actual:<1>`(finally가 completePurchase 호출)로 실패 확인 → fix 복원 후 GREEN.
- **게이트**: flutter analyze **No issues** · flutter test **+201**(194 baseline + 7 신규) 전체 통과.

### M8 테스트 정정(웨이브 내 회귀 발견·해소)
- 최초 `test_app_version.py`가 `importlib.reload(config_module)`로 전역 settings 싱글톤을 재생성 → rate_limit 등 import된 참조와 어긋나 `test_security.py::TestRateLimiting` 2건 + `test_error_responses` 1건을 **오염(전체 게이트에서 발견)**. → reload 제거, `Settings()` 새 인스턴스만으로 property 검증하도록 정정. 격리 실행으로 원인 확정 후 해소.

---

## Wave 1 소계 (게이트 결과)
- **백엔드**: `venv/bin/python -m pytest tests/` → **443 passed, 0 failed**(베이스라인 435 = 434 pass+1 known-fail → known-fail 그린화 + 신규 8테스트). `ruff check src/` pass. `alembic heads` 단일 `e5f6a7b8c9d0`.
- **모바일**: `flutter analyze` No issues · `flutter test` **201 passed**(194 + 7).
- **money 마이그레이션 실DB 리허설(M17)**: PG(postgres:16-alpine)에서 다중 active 시드 → upgrade dedup 결과 assert(u1=1 premium·cancelled 1·u2=1) + 부분유니크 2nd-active 차단 + cancelled 공존. ✅
- **NEEDS-DECISION**: 없음. **FALSE-GREEN-RISK**: 없음(3 크리티컬 전부 실 red→green 확인, C3는 stash로 화면 버그 실증). **이탈**: 없음.
- **사전존재 드리프트(스코핑)**: `alembic check`가 `books.retelling_source_book_id` FK 미기록 1건 보고 — W5 M10 대상. money-migration 공통 DoD `alembic check diff 0`는 M10 완료 시 충족.

---
