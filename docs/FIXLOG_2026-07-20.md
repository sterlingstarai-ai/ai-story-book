# FIXLOG — AI Story Book 출시 전 결함 수정 구현 (2026-07-20)

> 수임: 구현 전담 개발자 / 발주: CTO. 정본: `docs/DEV_SPEC_2026-07-20.md` v1.3.
> 이 파일이 진행 상태의 정본(요약 드리프트 금지 — FIXLOG·git log로 재동기화).
> 각 티켓: ID / 변경 파일 / 근본원인 / 추가 테스트명 / red→green 확인 / 게이트 결과 / 플래그(FALSE-GREEN-RISK·NEEDS-DECISION·이탈).

## 베이스라인 (2026-07-21 실측)
- 백엔드 pytest: **434 pass / 1 known-fail** (`test_shared_openapi_contract_is_committed_and_current` — 로컬 `apps/api/.env` `APP_VERSION=0.2.0` 잔재, info.version 드리프트). ruff pass.
- 모바일 flutter (3.38.7): 194 pass(문서값, W1 착수 시 재측정).
- 단일 alembic head: `d0e1f2a3b4c5`.
- 게이트 판정 기준: **"베이스라인 대비 회귀 0"** (전체 그린 아님). W1에서 M8-backend로 known-fail 그린화 후 이후 웨이브는 완전 그린 기대.
- 환경: venv Python 3.9.6, ruff 0.14.14, flutter 3.38.7, docker(PG 사용가능), alembic OK.

---

## Wave 1 — 치명 3 + M8 베이스라인 그린화

### M8 (백엔드 부분만, W1 승격) — app_version 코드 정본화
- **변경 파일**: `apps/api/src/core/config.py` (app_version: pydantic 필드 → 모듈 상수 `APP_VERSION` + `@property`), `apps/api/tests/test_app_version.py` (신규).
- **근본원인**: `app_version`이 pydantic Settings 필드라 `APP_VERSION` env(로컬 `.env`의 0.2.0 잔재)로 오버라이드 → 런타임 info.version 드리프트 → 계약 신선도 테스트 known-fail.
- **추가 테스트**: `test_app_version_not_overridable_by_env` — APP_VERSION=9.9.9 설정 후에도 app_version=='1.0.0'.
- **red→green**: RED `'9.9.9' == '1.0.0'` AssertionError → property 적용 후 GREEN. 베이스라인 known-fail(`test_shared_openapi_contract_is_committed_and_current`)도 동시 그린화.
- **게이트**: pytest test_app_version + test_openapi_contract 5 pass · ruff pass.
- **범위 메모**: M8의 모바일(package_info_plus)·CI/e2e(APP_VERSION 1.0.0) 부분은 §B4대로 **W6에 잔류**(W1은 베이스라인 그린화용 백엔드 정정만).

### M17 — subscriptions active 부분 유니크 + 동시 create 흡수
- **변경 파일**: `apps/api/src/models/db.py` (Subscription `__table_args__` 부분 유니크 `uq_subscriptions_active_per_user`), `apps/api/src/services/credits.py` (create_subscription: 취소 UPDATE 선-flush 순서 보장 + IntegrityError 2회 재시도 흡수), `apps/api/alembic/versions/e5f6a7b8c9d0_*.py` (신규: 기존 다중 active dedup 후 인덱스), `apps/api/tests/test_subscription_uniqueness.py` (신규).
- **근본원인**: `get_active_subscription` SELECT 후 active INSERT 사이에 DB 제약 부재 → 동시 두 호출이 active 2행 생성 → periodic_credits 영구 이중 지급.
- **추가 테스트**: `test_active_subscription_partial_unique_blocks_second`, `test_create_subscription_happy_upgrade_keeps_single_active`, `test_create_subscription_absorbs_race_integrity_error`.
- **red→green**: RED(부분유니크 미존재로 2행 허용 / 경쟁 시 2 active) → 제약+재시도 후 GREEN. 3 신규 + 기존 subscription/payment 29 pass.
- **플러시 순서 주의**: 새 제약 하에서 정상 업그레이드(basic→premium)도 취소 UPDATE를 새 active INSERT보다 먼저 flush하지 않으면 위반 → 취소 직후 `db.flush()` 삽입(happy-path 회귀 방지).
- **money 마이그레이션 실DB(PG) 리허설**: ephemeral postgres:16, 이전 head(d0e1f2a3b4c5)에서 u1 active 2행+u2 1행 시드 → `alembic upgrade head` → **dedup 결과 u1=1(premium=최신 유지)·cancelled 1·u2=1**, 2nd active insert는 `duplicate key ... uq_subscriptions_active_per_user`로 차단, cancelled 공존 OK. ✅
- **⚠ 사전 존재 드리프트(NEEDS-DECISION 아님, 스코핑 메모)**: `alembic check`가 `books.retelling_source_book_id` FK 미기록 드리프트 1건 보고 — **M17과 무관한 사전 존재분(= W5 M10 대상)**. M17 자체 마이그레이션은 diff 0(subscriptions 관련 diff 없음). money-migration 공통 DoD의 `alembic check diff 0`는 M10 완료 시 충족 예정.
- **게이트**: pytest 통과 · ruff 통과 · alembic heads 단일(e5f6a7b8c9d0) · SQLite+PG upgrade OK.

### C2 — 파이프라인 인-플라이트 최종 실패 시 크레딧 환불 배선
- **변경 파일**: `apps/api/src/services/orchestrator.py` (mark_job_failed), `apps/api/src/services/tasks.py` (_mark_job_failed_async), `apps/api/tests/test_payment_integrity.py` (신규 4테스트).
- **근본원인**: 선차감(reference_id=job_id) 후 정상 실패 경로(orchestrator.mark_job_failed / celery _mark_job_failed_async)가 refund_for_job을 호출하지 않고 job_monitor 스캔은 이미 failed인 잡을 제외 → 유료 크레딧 영구 소실.
- **핸드오프 정정 MA3 적용(원 fix보다 우선)**: refund_for_job(commit=False)이 add_credits의 무조건 rollback을 타면 미커밋 job.status='failed'까지 삼켜 DoD 불가 → **(a) 실패 status 먼저 commit → 별도 트랜잭션에서 refund_for_job(commit=True)**. user_key는 commit 전 캡처. 환불은 try/except로 감싸 실패가 잡 마킹을 막지 않고 logger.warning만.
- **G3 결정 반영**: 전 실패 코드 환불(관대) — SAFETY_INPUT 예외 분기 미추가.
- **추가 테스트**: `test_orchestrator_mark_job_failed_refunds_charged_job`, `test_mark_job_failed_refund_idempotent`, `test_tasks_mark_job_failed_async_refunds`, `test_mark_job_failed_persists_status_even_if_refund_fails`(MA3 DoD).
- **red→green**: RED(3 core: 잔액 2 유지=미환불) → 배선 후 GREEN. MA3 가드 테스트는 환불 강제실패 시 status='failed' 영속·잔액 2 확인.
- **게이트**: pytest test_payment_integrity 8 pass · ruff pass.
- **폭발 반경 메모**: mark_job_failed는 W3 H10(fence)·M23(done-fence) 공유 — C2의 refund 삽입을 먼저 착지(§3.2)했으니 H10이 그 위에 조건부 UPDATE fence를 얹을 것.

### C3 — IAP 검증 실패 시 finally completePurchase 무조건 호출(소모성 대금 유실)
- **변경 파일**: `apps/mobile/lib/services/iap_service.dart` (buyProduct autoConsume:false + finishPurchase 플랫폼 라우팅 + androidConsume 시임), `apps/mobile/lib/screens/credits_screen.dart` (_handlePurchaseUpdate 재구성), `apps/mobile/pubspec.yaml`(+in_app_purchase_android ^0.4.0)·`pubspec.lock`, `apps/mobile/test/iap_purchase_flow_test.dart`(신규 7테스트).
- **근본원인**: `_handlePurchaseUpdate`의 finally가 verifyIap 성공 여부와 무관하게 completePurchase 호출 → 검증 실패(순단·5xx) 시 트랜잭션 finish/consume·크레딧 미지급·구제경로 전무.
- **수정**: finally에서 complete 제거 → verify 성공 직후에만 `finishPurchase`. catch 시 미완료(pending 유지)로 다음 실행 재검증. buyConsumable autoConsume:false.
- **핸드오프 정정 B1 적용(원 fix보다 우선)**: autoConsume:false + completePurchase만으로는 Android에서 consume이 영영 안 돼 재구매 ITEM_ALREADY_OWNED 영구 실패 → **verify 성공 시 Android는 `InAppPurchaseAndroidPlatformAddition.consumePurchase` 명시 호출**(iOS는 completePurchase). 테스트 시임 `androidConsume`로 라우팅 검증.
- **핸드오프 정정 MI2 적용**: 723행 in-flight 중복 이벤트 가드는 complete 호출 금지 → drop만.
- **l10n**: 신규 문자열 없음 — 기존 `creditsVerifyFailed`('잠시 후 다시 시도' 취지) 재사용. gen-l10n 불필요.
- **추가 테스트**: buyProduct autoConsume:false / buyNonConsumable / finishPurchase 3-라우팅(iOS complete·Android consume·Android non-consumable complete) / 화면 제어흐름 verify 실패→미완료·성공→완료 1회.
- **red→green**: 화면 버그 RED를 `git stash push credits_screen.dart`로 원 버그 복원 후 실증 — `verifyIap failure` 테스트가 `Expected:<0> Actual:<1>`(finally가 completePurchase 호출)로 실패 확인 → fix 복원 후 GREEN.
- **게이트**: flutter analyze **No issues** · flutter test **+201**(194 baseline + 7 신규) 전체 통과.

### M8 테스트 정정(웨이브 내 회귀 발견·해소)
- 최초 `test_app_version.py`가 `importlib.reload(config_module)`로 전역 settings 싱글톤을 재생성 → rate_limit 등 import된 참조와 어긋나 `test_security.py::TestRateLimiting` 2건 + `test_error_responses` 1건을 **오염(전체 게이트에서 발견)**. → reload 제거, `Settings()` 새 인스턴스만으로 property 검증하도록 정정. 격리 실행으로 원인 확정 후 해소.

---

## Wave 1 소계 (게이트 결과)
- **백엔드**: `venv/bin/python -m pytest tests/` → **443 passed, 0 failed**(베이스라인 435 = 434 pass+1 known-fail → known-fail 그린화 + 신규 8테스트). `ruff check src/` pass. `alembic heads` 단일 `e5f6a7b8c9d0`.
- **모바일**: `flutter analyze` No issues · `flutter test` **201 passed**(194 + 7).
- **money 마이그레이션 실DB 리허설(M17)**: PG(postgres:16-alpine)에서 다중 active 시드 → upgrade dedup 결과 assert(u1=1 premium·cancelled 1·u2=1) + 부분유니크 2nd-active 차단 + cancelled 공존. ✅
- **NEEDS-DECISION**: 없음. **FALSE-GREEN-RISK**: 없음(3 크리티컬 전부 실 red→green 확인, C3는 stash로 화면 버그 실증). **이탈**: 없음.
- **사전존재 드리프트(스코핑)**: `alembic check`가 `books.retelling_source_book_id` FK 미기록 1건 보고 — W5 M10 대상. money-migration 공통 DoD `alembic check diff 0`는 M10 완료 시 충족.

**W1 커밋**: `fab7bd8` fix(w1): block infinite monetization... (푸시 안 함).

---

## Wave 2 — C1 완결 + IAP 영수증·웹훅·구독 정합의 돈 코어  _(진행 중)_

> §3.2 권장 순서로 진행: create_subscription = M17(완료)→M13→C1→H5; webhook = H4→H5→M14.

### M13 — POST /credits/subscribe(plan=free)가 활성 유료 구독 즉시 소멸
- **변경 파일**: `apps/api/src/services/credits.py` (create_subscription: 기존 활성 취소 시 `current_period_end=now` 즉시만료 제거), `apps/api/src/routers/credits.py` (subscribe: 활성 유료+plan=free → APIError 400 `ACTIVE_PAID_SUBSCRIPTION`; existing 조회 앞으로 이동·중복 제거), `apps/api/tests/test_payment_security.py` (신규 2테스트).
- **근본원인**: 유료 결제 가드가 plan!='free'에만 적용 → free는 항상 통과. create_subscription이 기존 활성 구독을 즉시 current_period_end=now로 종료 → 결제한 잔여기간 소멸(스토어 청구는 계속).
- **G5 결정 반영**: 즉시 4xx 거부·크레딧 미지급.
- **추가 테스트**: `test_free_subscribe_rejected_when_active_paid_sub`(400·구독 불변·잔여기간 보존), `test_create_subscription_keeps_prev_period_end`(cancelled 잔여기간 유지·active 1행).
- **red→green**: RED(period_end=now / free 200) → 제거+거부 후 GREEN.
- **회귀**: 업그레이드(basic→premium) get_active_subscription==premium·active 1행 유지. M17 부분유니크·flush순서 정합 확인(28 pass).
- **게이트**: pytest 28 pass(payment_security+subscription_uniqueness+periodic+iap_hardening+credits_transactions) · ruff pass.
- **폭발 반경 메모**: current_period_end=now 제거는 C1(이전소유자 만료 범위)·periodic_credits(만료 기준 리필)와 공유 — C1에서 MA4(만료 범위 축소) 결합 검토 필요.

### M16 — credit_transactions 멱등성 DB 강제 (refund/purchase 부분 유니크)
- **변경 파일**: `apps/api/src/models/db.py` (CreditTransaction __table_args__: uq_refund on (reference_id) WHERE type='refund', uq_purchase on (user_key,reference_id) WHERE type='purchase', ix_reference_type), `apps/api/src/services/credits.py` (add_credits rollback commit=True 한정 + refund_for_job 세이브포인트·_job_already_refunded 헬퍼), `apps/api/src/routers/credits.py` (admin add IntegrityError 흡수 멱등), `apps/api/alembic/versions/f6a1b2c3d4e5_*.py` (신규: dedup+인덱스), `apps/api/tests/test_payment_integrity.py` (신규 4테스트).
- **근본원인**: refund/purchase 멱등이 앱 check-then-write에만 의존(DB 제약 없음) → 멀티 레플리카 동시 스캔·admin 재전송 시 이중 환불·N중 지급. reference_id 인덱스 부재.
- **⚠ CTO W1 감사 포워드리스크(MA3) 처리**: `add_credits`의 무조건 `db.rollback()`을 **commit=True에만 한정**(commit=False는 예외를 호출자에 위임 — clawback_credits 기존 패턴과 정렬). `refund_for_job`은 `begin_nested()` 세이브포인트로 add_credits를 감싸 동시 IntegrityError를 흡수 → 호출자(job_monitor 등 commit=False)의 미커밋 job.status를 폐기하지 않음. **이로써 create_subscription/refund/add_credits의 commit=False 경로가 IAP 트랜잭션을 파괴하지 않는 기반 완성**(create_subscription 세이브포인트는 C1에서 완결).
- **추가 테스트**: `test_refund_partial_unique_blocks_duplicate`, `test_purchase_partial_unique_blocks_duplicate`(NULL·타유저 공존 포함), `test_refund_for_job_absorbs_db_duplicate`(시임 `_job_already_refunded` monkeypatch로 앱체크 미스 강제→DB 흡수), `test_admin_add_duplicate_transaction_id_idempotent`(엔드포인트, +10 1회).
- **red→green**: RED(4건: 2행 허용·23≠13) → 제약+흡수 후 GREEN. 36 pass(payment_integrity+iap_hardening+payment_security+credits_transactions+subscription_uniqueness).
- **money 마이그레이션 실DB(PG) 리허설**: postgres:16, refund 3중·purchase 2중 시드 → upgrade dedup **refund→1·purchase→1(min id 유지)**, 중복 insert `uq_credit_transactions_refund` 차단, NULL reference_id refund 공존 OK. `alembic check`는 사전존재 retell FK 드리프트만(credit_transactions/subscriptions diff 0). ✅
- **게이트**: pytest 통과 · ruff 통과 · alembic heads 단일 `f6a1b2c3d4e5` · SQLite+PG upgrade OK.

### H4 — IAP 웹훅 미기록/선도착 트랜잭션 처리(orphan 적재 + store_txn 매칭 + sticky 재적용)
- **변경 파일**: `db.py`(IapWebhookEvent 모델), `alembic/.../a1b2c3d4e5f6_*.py`(신규 테이블), `routers/iap.py`(_apply_webhook_status 재설계: transaction_id OR store_transaction_id 매칭·Google suffix 정규화·orphan 적재·_apply_status_to_receipt/_reapply_orphan_events 헬퍼·sticky rank), `tests/test_iap_hardening.py`(신규 4).
- **근본원인**: transaction_id 단일 조회로 store 식별자/선도착 환불 통지가 미스→200 ignored→provider 재시도 사멸→환불된 구독이 active로 남아 영구 리필.
- **결정 반영**: G4(orphan 적재 후 200 + verify 시 sticky 재적용). **정정 MI1**: orphan 재적용을 refund/expired만이 아니라 cancelled 포함 전 상태 + created_at 순 + sticky 우선순위(refunded/expired>cancelled>active) rank로.
- **추가 테스트**: store_txn 매칭 만료 / orphan 보존·중복 멱등 / 선도착 환불 sticky 재적용(event.applied) / 환불 후 active 미복귀. red→green 확인.
- **게이트**: 마이그레이션 SQLite+PG upgrade OK, 단일 head.

### C1 — 복원 무한 수익화 차단 (헤드라인 치명)
- **변경 파일**: `services/credits.py`(create_subscription: **grant_credits 파라미터 + begin_nested 세이브포인트 + pre-savepoint flush**; expire_active_subscription_for_plan 헬퍼), `services/iap_verifier.py`(IAPVerificationResult.expires_date_ms), `routers/iap.py`(restore: with_for_update·이전소유자 만료·grant_credits=False·MA1 만료검사·try/except; new-sub MA1 가드; _subscription_expired 헬퍼), `tests/test_iap_hardening.py`·`test_subscription_uniqueness.py`(신규).
- **근본원인**: restore 분기가 매 호출 월간 크레딧을 새 subscription.id ref로 재지급(무제한) + 이전소유자 구독 미만료 + 락/CAS 없음.
- **결정 반영**: G1(재지급 금지 grant_credits=False), G2/**정정 MA4**(이전 소유자 **해당 plan active만** 만료, cancelled 잔여기간 보존 — 일괄 만료 금지), **정정 MA1**(만료 영수증 restore→active 미생성: iap_verifier expires_date_ms 검사), **정정 MA8**(동시성 시임 red 실증).
- **⚠ CTO W1 감사 포워드리스크 처리(핵심)**: create_subscription의 옛 무조건 `db.rollback()`을 **begin_nested 세이브포인트**로 교체 → commit=False(IAP restore) 트랜잭션 내 동시 race 시 호출자의 미커밋 UPDATE(영수증 권한 이전)가 폐기되지 않음. **red→green 실증**: 세이브포인트를 옛 full-rollback으로 되돌리면 `saved.user_key=='fwd-old-owner'`(권한 이전 소실)로 RED, 세이브포인트로 GREEN. pre-savepoint flush는 방어적 보강.
- **추가 테스트**: restore 재지급 없음 / 이전소유자 해당 plan 만료 / 만료 영수증 active 미생성 / 세이브포인트 outer-tx 보존. 
- **게이트**: money 마이그레이션 없음(C1은 코드+iap_verifier). money 회귀 46 pass.

### H5 — 웹훅이 해당 영수증 구독만 갱신 (IAPReceipt.subscription_id)
- **변경 파일**: `db.py`(IAPReceipt.subscription_id FK+index), `alembic/.../b2c3d4e5f6a7_*.py`(신규 컬럼+index+**PG FK**), `routers/iap.py`(verify 3분기 subscription_id 세팅 + _apply_status_to_receipt: subscription_id 타깃·레거시 plan 폴백), `tests/test_iap_hardening.py`(신규).
- **근본원인**: 영수증→구독 FK 부재로 '최신 구독' 임의 매칭 → 업그레이드 후 옛 영수증 통지가 방금 결제한 구독을 죽임.
- **추가 테스트**: `test_webhook_targets_receipt_subscription_not_latest`(옛 basic 통지가 최신 premium 미영향). red→green.
- **마이그레이션 정정**: 최초 add_column이 FK 누락 → alembic check가 iap_receipts.subscription_id FK 드리프트 검출 → **PG에 op.create_foreign_key 추가**(SQLite는 create_all로 FK 포함). 재검증 후 alembic check는 사전존재 M10 retell FK만 남음.
- **게이트**: SQLite+PG upgrade OK, 단일 head.

### M14 — 구독 환불 시 지급 월간 크레딧 clawback
- **변경 파일**: `routers/iap.py`(_apply_status_to_receipt에 SUBSCRIPTION_PRODUCTS refund clawback), `tests/test_iap_hardening.py`(신규).
- **근본원인**: clawback이 CREDIT_PACK만 → buy→30크레딧→refund 사이클마다 무비용 적립. **결정 G6**: 0클램프 부분 회수.
- **추가 테스트**: `test_refund_webhook_claws_back_subscription_credits`(30→0, 멱등). red→green.

### L8 — get_or_create_credits/streak PK 충돌 500 흡수
- **변경 파일**: `services/credits.py`(get_or_create_credits begin_nested 흡수·재조회), `services/streak.py`(get_or_create_streak IntegrityError 흡수), `tests/test_payment_integrity.py`(신규 2).
- **추가 테스트**: 별도 세션 racing 시임으로 실제 PK 충돌 유발 → 흡수·행/보너스 이중 없음. red→green(시임 유발).

### L10 — 운영 Sandbox 영수증 차단 + 웹훅 토큰 헤더
- **변경 파일**: `core/config.py`(review_sandbox_allowlist), `routers/iap.py`(Sandbox 지급 차단·allowlist 예외·_require_webhook_secret X-Webhook-Token 헤더 우선·쿼리 폴백), `packages/shared/schema/openapi.json`(재export: X-Webhook-Token 헤더), `tests/test_iap_hardening.py`(신규 3).
- **결정 G8**: REVIEW_SANDBOX_ALLOWLIST 상품만 허용.
- **⚠ 테스트 함정 발견·수정**: 최초 sandbox-blocked 테스트가 잘못된 이유로 pass(비-UUID user_key → 운영 UUID 검증 400). 유효 UUID로 교정해 실제 sandbox 차단 400을 검증(`sandbox in body`).
- **계약**: openapi.json 재export·신선도 통과(서버-서버 웹훅, 모바일 소비자 없음).
- **추가 테스트**: sandbox 차단(유효 UUID)·allowlist 예외·헤더 토큰. red→green.

---

## Wave 2 소계 (게이트 결과)
- **백엔드**: `pytest tests/` → **464 passed, 0 failed**(베이스라인 445 + W2 신규 ~19). `ruff` pass. `alembic heads` 단일 `b2c3d4e5f6a7`.
- **모바일**: `flutter test` **201 passed**(W2 백엔드-only, 회귀 0).
- **계약**: L10 X-Webhook-Token 헤더로 openapi.json 재export·커밋, 신선도 테스트 통과.
- **money 마이그레이션 실DB(PG) 리허설**: M16(dedup refund3→1·purchase2→1·중복차단·NULL공존) ✅. H4(iap_webhook_events 생성)·H5(subscription_id 컬럼+PG FK+index) full-chain upgrade OK, `alembic check`는 **사전존재 M10 retell FK만**(내 마이그레이션 diff 0).
- **⚠ CTO W1 감사 포워드리스크(commit=False rollback이 IAP 트랜잭션 파괴)**: create_subscription 세이브포인트 + add_credits/refund_for_job rollback commit=True 한정 + refund_for_job/get_or_create_credits 세이브포인트로 **해소**. 세이브포인트가 load-bearing임을 red→green으로 실증.
- **NEEDS-DECISION**: 없음. **FALSE-GREEN-RISK**: 없음(전 티켓 red→green; L10 함정 자가검출·수정). **이탈**: 없음.
- **사고·복구**: RED 실증 스크립트의 `git checkout -- credits.py`가 미커밋 M13/M16/C1 변경을 되돌림 → 즉시 감지·전량 재적용·48 pass 재확인. 이후 RED 실증은 텍스트 패치만 사용(git 미사용).

**W2 커밋**: `4d0933b` fix(w2): IAP receipt/webhook/subscription money core (푸시 안 함).

---

## Wave 3 — 돈/POD 정합 + 오케스트레이터 크레딧 fence + 모바일 이중차감  _(완료)_

### H9 — run_step 재시도 가능 LLMError 재시도 + 최종 에러코드 보존
- **변경 파일**: `services/orchestrator.py`(run_step: `except StoryBookError as e` → is_retryable 분기; 최종 실패 시 StoryBookError code 보존; is_retryable import), `tests/test_orchestrator.py`(신규 2), `tests/test_chaos.py`(TransientError→StoryBookError 정정).
- **근본원인**: `except StoryBookError: raise`가 LLMError(429/JSON불량)를 재시도 루프 우회해 즉시 실패. is_retryable 데드코드. 최종 실패가 UNKNOWN으로 뭉갬(규범표 위반).
- **추가 테스트**: `test_run_step_retries_retryable_llm_error`(call_count==2), `test_run_step_preserves_final_error_code`(LLM_TIMEOUT 보존), test_chaos 정정. red→green. 기존 SAFETY no-retry 회귀 유지.
- **폭발반경 메모**: run_step은 W5 M20과 공유 — 최종코드 보존은 M20-4와 동일, M20은 참조만.

### H10 — 잡 write-back fence + 완료 후 환불 clawback(MA2)
- **변경 파일**: `services/orchestrator.py`(update_job_status·mark_job_failed·mark_job_done 조건부 UPDATE fence), `services/job_monitor.py`(_mark_job_failed 조건부 UPDATE), `tests/test_payment_integrity.py`(신규 4).
- **근본원인**: fence 없는 write-back으로 SLA 실패+환불 잡을 워커가 done으로 뒤집어 '책+환불' 이중지급. job_sla(600)<time_limit(720).
- **⚠ 정정 MA2 적용(핵심)**: 라이브러리는 job.status로 필터하지 않음(user_key만) → fence로 status만 막으면 책은 여전히 배달됨(false-green) → **mark_job_done에서 rowcount 무관하게 refund 존재 시 clawback**(책 배달분 과금, 멱등). G13(코드 fence만).
- **결정 반영**: mark_job_failed는 done 미뒤집기 + 전이 성공(rowcount==1) 시에만 환불(C2 위에 fence). job_monitor도 동일.
- **추가 테스트**: done-fence(failed 유지+clawback)·완료후 clawback·update terminal skip·mark_failed done skip. red→green.
- **폭발반경 메모**: mark_job_done fence는 W5 M23(done 멱등)과 목표 공유 — M23은 산출물 멱등화 집중.

### H6 — POD 주문 멱등·orphan·unknown-outcome
- **변경**: `db.py`(PodOrder idempotency_key + provider_total/currency + (user_key,idempotency_key) 부분유니크), `alembic/c3d4e5f6a7b8`(가법적), `routers/pod.py`(get_idempotency_key 수용·멱등 조회·**fail-closed 선기록(pending_submit)→외부생성→갱신**·IntegrityError 재조회), `services/pod_provider.py`(PodSubmitUnknown·타임아웃→submit_unknown·reconcile_by_external_id), `api_client.dart`·`pod_order_screen.dart`(idempotencyKey), 테스트(백엔드 5·모바일 1).
- **근본원인**: 멱등키 미수용·외부생성 선행(orphan)·타임아웃 확정실패 삼킴. **정정 MI4**: prewrite 검증 스텁을 별도 세션(커밋행만 가시). red→green·계약 재export.

### H12 — Printful payload 완성 (recipient·아트워크·hybrid 비위장)
- **변경**: `pod_provider.py`(recipient city/state_code/address2 + US/CA 필수·items.files PDF(G20)·미첨부→pending_artwork·hybrid 실패→pending_provider/local_fallback), `pod.py`(US/CA state 400 선검증·pdf_url 전달), `pod_order_screen.dart`(city/state/line2), l10n 5키 ko/en/ja+gen-l10n, 테스트. **G20**. red→green.

### H13 — POD 통화 오염(×1300) 제거 (G7 quoted vs provider 분리)
- **변경**: `pod_provider.py`(_cost_to_krw 제거·`_amount_to_cents` cents·PodCreateResult provider_total/currency), `pod.py`(지역견적 유지·provider 실비 별도 컬럼). **G7**. red→green(2500≠32500).

### L6 — POD GET sync 견고화
- **변경**: `pod.py get_pod_order`(provider 장애 시 로컬 스냅샷·변경없으면 commit 생략·종결상태 sticky·submit_unknown reconcile), `pod_provider.py`(_resolve_mode). 최소변경.

### L11 — POD config fail-open 제거
- **변경**: `pod_provider.py`(_resolve_mode 무효 pod_mode 경고·_store_id_int 안전파싱). **G33 재량**.

### H20 — POD 모바일 KRW 하드코딩 → 서버 quote
- **변경**: `pod.py`(GET /v1/pod/quote 신규 단일소스), `api_client.dart getPodQuote`, `pod_order_screen.dart`(하드코딩 제거·국가/수량 변경 시 서버견적·통화 표시), l10n {currency} 플레이스홀더 ko/en/ja+gen-l10n, 계약, 테스트. red→green.

### H18 — 생성 POST 멱등키 미전송 이중차감
- **변경**: `books.py create_series_next`·`streak.py generate_today_story`(get_idempotency_key·Job dedup), `providers.dart BookCreationNotifier`(spec 서명 기반 시도키 재사용·성공 리셋), `api_client.dart`(series/today idempotencyKey), `library_screen.dart`·`home_screen.dart`+`todayAttemptKeyProvider`, 계약, 테스트(백엔드 today dedup·모바일 재시도 재사용). red→green.
- **정정 MA7 스코핑**: retell/from-photo/from-drawing 서버측 멱등은 그 엔드포인트를 소유하는 **H17(W4)로 이관**(클라+서버 동시). series/today는 수용했으므로 문서화(미수용 헤더 문서화 금지 원칙 준수).

## Wave 3 소계 (게이트 결과)
- **백엔드**: `pytest tests/` → **480 passed, 0 failed**(회귀 0). ruff pass. 단일 head `c3d4e5f6a7b8`.
- **모바일**: `flutter analyze` No issues · `flutter test` **204 passed**(201+3). gen-l10n ko/en/ja 파리티.
- **계약**: pod idempotency·/v1/pod/quote·series/today idempotency 재export, 신선도 통과.
- **money 마이그레이션 실DB(PG)**: pod_orders idempotency_key+provider 금액컬럼+부분유니크 additive upgrade OK, `alembic check` 사전존재 M10 retell FK만.
- **H10 MA2 핵심**: 라이브러리가 job.status로 필터 안 함을 실증 → fence만으론 '책+환불' 미차단 → mark_job_done clawback 배선(false-green 회피).
- **NEEDS-DECISION**: 없음. **FALSE-GREEN-RISK**: 없음. **이탈**: MA7 스코핑(H17/W4 이관, 명시).

**W3 커밋**: `89ff08b` fix(w3): POD money integrity... (푸시 안 함).

---

## Wave 4 — 글로벌 i18n(생성·안전·오디오·스트릭) + 모바일 법정 동의/삭제 + 파이프라인 안전  _(진행 중)_

### L16 — language_display_name silent ko 폴백 제거 + 학습자산 언어명 단일출처화
- **변경**: `core/i18n.py`(미지원 코드 → ValueError raise), `services/llm.py`(call_learning_assets 중복 {ko,en,ja} 맵 제거 → i18n.language_display_name 단일출처 → zh/es가 中文/Español로 정상), `tests/test_i18n.py`(폴백 기대 → raise 기대, zh/es 표시명, 학습자산 소스 단일출처 확인).
- **근본원인**: `.get(code, ko)` 조용한 폴백으로 enum 추가·맵 갱신 누락 시 신규 언어 책이 전부 ko로 조용히 생성될 잠복. llm 자체 맵에 zh/es 없어 코드 문자열이 프롬프트에 유입.
- **red→green**: RED('zz'→'한국어' DID NOT RAISE / 소스에 '日本語' 잔존) → 수정 후 GREEN. 전 호출부 enum.value 확인(§3.2).

### H21 — 계정 삭제 확인 키워드 '삭제' 하드코딩(en/ja 삭제 불가)
- **변경**: `screens/settings_screen.dart`(비교식 `== '삭제'` → `== l.settingsDeleteKeyword`·프롬프트에 keyword 전달), l10n settingsFinalConfirmPrompt ko/en/ja `{keyword}` 플레이스홀더+gen-l10n, `test/settings_delete_keyword_test.dart`(신규).
- **근본원인**: 표시 키워드는 로캘화(en 'Delete'·ja '削除')인데 비교는 '삭제' 하드코딩 → GDPR/PIPA 삭제권·App Store 5.1.1 위반. ja 프롬프트는 '삭제' 안내인데 키워드 '削除'로 자체 모순.
- **red→green**: 로캘별 키워드==비교대상 정합 + en/ja 프롬프트에 '삭제' 부재 검증. (전체 삭제 플로우 위젯테스트는 부모인증게이트로 비실용 → l10n 정합 테스트로 본질 검증.)
- **게이트**: flutter analyze No issues · 신규 테스트 pass.

### M18 — job_monitor 좀비 재큐 제거 → 즉시 실패+환불
- **변경**: `services/job_monitor.py`(_handle_stuck_job 재큐 분기 제거 → _mark_job_failed 즉시), `tests/test_job_monitor.py`(신규 2).
- **근본원인**: Job 행에 spec 미저장·Celery는 브로커 소비라 재디스패치 구조적 불가 → 'queued' 재큐가 좀비(거짓 '재시도 중 n/3'). **G14**(즉시 실패+환불, 복구는 재생성). red→green(queued→failed·환불1회·재큐 없음).

### H2 — 하루/월 경계 KST 고정 → 사용자별 IANA 타임존 (G10, 스코프증가 L)
- **변경**: `core/utils.py`(4 헬퍼 offset덧셈 → **zoneinfo + tz 파라미터**, DST 안전; 기본 Asia/Seoul 하위호환), `models/db.py`(UserSettings.timezone), `alembic/d4e5f6a7b8c9`(server_default 백필), `services/streak.py`(`load_user_tz` 헬퍼 + record_reading/get_streak_info/_get_profile_streak tz 스레딩), `routers/books.py`(월/일 한도 tz), `routers/settings.py`(PATCH/GET timezone + IANA 검증), 계약, `tests/test_timezone_boundaries.py`+settings tz 테스트.
- **결정 G10**: 사용자별 타임존(창업자 CTO권장 KST고정과 다름 — W4 스코프증가 감수). get_today_story 전역 회전은 기본 tz 유지.
- **정정 MA10**: record_reading utcnow 하드코딩 → 테스트가 `src.services.streak.utcnow` + `src.core.utils.utcnow` 양쪽 monkeypatch로 결정적 시각 고정. **정정 B3**: down_revision 현재 head(c3d4e5f6a7b8) 체이닝.
- **red→green**: LA 사용자 월07:00·화09:00(PDT) → current_streak==2(KST 고정이면 days_since=2로 리셋). 경계 헬퍼 tz별 상이 검증.
- **모바일 잔여(follow-on)**: flutter_timezone 자동감지·온보딩 전송은 소규모 후속 — 서버는 PATCH /v1/settings로 timezone 수용(계약 완료), 모바일 전송 배선만 남음. FIXLOG 명시.
- **게이트**: 백엔드 487 pass · ruff · 단일 head `d4e5f6a7b8c9` · SQLite upgrade OK · 계약 재export 신선도 통과. L16 raise 회귀 0(전 호출부 enum.value 확인).

### M31 — call_text_rewrite 스키마 검증 없는 raw json.loads + revised_text 누락 조용한 성공
- **변경**: `models/dto.py`(RewriteResult 모델), `services/llm.py`(call_text_rewrite json.loads → parse_json_response(RewriteResult): 펜스 제거·검증·누락 시 LLM_JSON_INVALID), `services/orchestrator.py`(regenerate_page `.get('revised_text', page.text)` → `.revised_text`), `tests/test_orchestrator.py`(신규 TestRewriteValidation 3).
- **근본원인**: anthropic 펜스 출력 → JSONDecodeError, revised_text 누락 → 원문 유지 채 '재생성 완료'(조용한 no-op). 규범 'LLM 출력 무조건 검증' 위반.
- **red→green**: 펜스 파싱 OK·누락 필드 LLM_JSON_INVALID·소스에서 parse_json_response 사용·.revised_text 접근 확인. H9(재시도)와 정합 — LLM_JSON_INVALID 실제 재시도. **§3.2**: M31 먼저(반환타입 확정) → H23 나중.

### L17 — 오늘의 동화 Theme 역매핑 4/7 조용한 실패 → theme=None
- **변경**: `models/dto.py`(Theme enum에 courage/kindness/growth/imagination 정식 추가, G11), `routers/streak.py`(한국어 name 역매핑 `Theme(name)` → 테마 코드 멤버명 직접 매핑 `Theme.__members__.get(code)`, 매핑불가 시 logger.warning+emotion 기본값, 조용한 None 제거; logger 추가), 계약(Theme enum 재export), `tests/test_today_generate.py`(신규 2).
- **red→green**: 7개 일일 테마 전부 Theme 멤버 매핑 확인 + courage 날 생성 성공. **G11 결정**: Theme enum 정식 추가.

### H14 — daily_stories.date 유니크 부재 레이스 → 하루종일 전사용자 500 (H26 통합)
- **변경**: `models/db.py`(DailyStory UniqueConstraint('date'), index=True 제거), `alembic/e5f6a7b8c9da`(dedup 후 batch 유니크 — SQLite ALTER 제약 우회), `services/streak.py`(get_today_story: 방어적 first()·INSERT IntegrityError rollback 후 재조회 멱등), `tests/test_today_generate.py`(신규 2).
- **근본원인**: 트랜잭션 밖 check-then-insert + non-unique 인덱스 → 멀티레플리카 버스트에 중복 행 → scalar_one_or_none MultipleResultsFound 500. **H26은 동일 버그 — H14로 단일화(§3.2)**.
- **정정 B3**(down_revision 현재 head 체이닝)·**MA8**(유니크 경쟁 시임 + 직접 중복 IntegrityError 분리).
- **red→green**: 유니크 2행 IntegrityError·race 흡수(경쟁행 반환·중복 없음). **money 마이그레이션 실DB(PG)**: 3중복 시드 → dedup=1(min id)·2nd insert `uq_daily_stories_date` 차단·alembic check M10만. ✅
- **게이트**: 백엔드 494 pass · ruff · 단일 head `e5f6a7b8c9da` · SQLite batch upgrade OK.

### H19 — 시리즈 '다음 권'이 원작 스타일·연령대 버리고 watercolor/5-7로 생성
- **변경**: `models/dto.py`(SeriesNextRequest style/target_age → Optional=None, G22), `services/orchestrator.py`(start_series_generation effective_style/effective_target_age 상속: 명시값→prev_book→기본), `routers/books.py`(무료한도 검사에 effective style), `mobile api_client.dart`(createSeriesBook style/targetAge/language 파라미터+payload), `mobile library_screen.dart`(latest.style/targetAge 전달), 계약(SeriesNextRequest nullable), 테스트(모바일 style/target_age/language 전송).
- **근본원인**: 모바일이 characterId/topic만 전송, SeriesNextRequest 기본값 watercolor/5-7 사용, 백엔드는 language만 상속. **G22**: Optional 승인.
- **red→green**: 모바일 style='3d'/target_age='7-9'/language='en' 전송 검증. 백엔드 Optional 수용·기존 series 8테스트 회귀 0.
- **게이트**: 모바일 206 pass · 백엔드 series 8 pass · ruff · 계약 재export 신선도 통과.

### M34 — 골든 하니스 ja/zh/es 미검증 + mock zh/es 미표현 false-green
- **변경**: `services/llm.py`(mock 언어감지 하드코딩('en','ja','ko') → i18n.SUPPORTED_LANGUAGES 단일출처), `docs/qa/golden-prompts.json`(ja/zh/es 엔트리 추가 — 5언어 전부), `tests/test_golden_prompts.py`(신규 2: 언어 커버리지·mock zh/es 반영).
- **근본원인**: 골든이 ko×3+en×1뿐이라 ja/zh/es 파이프라인 미게이트 GA. mock이 zh/es를 ko로 폴백해 language_matches_spec 실패 → 게이트 미배선(false-green). i18n와 mock 목록 드리프트.
- **red→green**: 기존 harness 게이트가 이제 ja/zh/es 완주(mock 반영 요구) + zh/es 언어 반영·5언어 커버 확인.

### M21 — 사진/그림 캐릭터 분석 파싱 실패 시 고정 mock 저장 (fail-open)
- **변경**: `services/photo_character.py`(_analyze_with_openai·_analyze_with_anthropic의 `except JSONDecodeError: return _mock_analysis()` → `raise`; mock 프로바이더 정상 경로는 유지), `tests/test_photo_character.py`(신규 3).
- **근본원인**: 실 프로바이더 vision 파싱 실패를 고정 '양갈래 소녀' mock으로 200 저장 → 부모가 올린 사진과 전혀 다른 캐릭터. 형제 텍스트 경로는 raise인데 vision만 비대칭 fail-open. 라우터는 이미 5xx 래핑.
- **red→green**: openai/anthropic 비-JSON → raise, mock 프로바이더 유지. ruff clean.

### M19 — 이미지 부분 실패 게이트 구멍(빈 URL 페이지 경고 누락)
- **변경**: `core/book_assets.py`(build_generation_warnings: placeholder뿐 아니라 빈/None URL도 cover_image_missing/page_image_missing 경고 — asset_status 'missing'과 일관), `tests/test_book_assets_warnings.py`(신규 4).
- **근본원인**: 예외 실패로 image_url='' 저장 시 warnings 0건 → 그림 없는 페이지가 경고 배너 없이 '완성' 배달. placeholder는 경고·빈URL은 침묵하는 비대칭. **G15**: 경고+degraded 배달(표지 placeholder 허용) — 잡 실패 아님.
- **red→green**: 빈 페이지/표지 URL 경고 + placeholder 회귀 유지 + 정상 무경고. 계약은 warning code가 자유문자열이라 불변(신선도 통과).

### M28 — 캐릭터 시트 master_description '한글로' 하드코딩 → 이미지 프롬프트 영어 고정
- **변경**: `prompts/generate_character_sheet.system.jinja2`('한글로' → '영어로', 언어 무관 명시), `prompts/generate_image_prompts.system.jinja2`(positive_prompt 영어로만 명시), `tests/test_i18n.py`(신규 2).
- **근본원인**: en/ja/zh/es 책의 master_description이 한국어로 작성돼 DALL-E에 한국어 프롬프트 혼입(negative_prompt는 'korean text' 배제와 모순). photo_character는 이미 영어 — 캐릭터 시트만 비대칭. **G31**: master_description 영어 통일(3경로 정합).
- **red→green**: 렌더 프롬프트에 '한글로' 부재·'영어로' 존재. 계약/l10n 불변.

### M29 — 입력 모더레이션 언어 미전달 → 비한국어 사용자에 한국어 차단 사유 노출
- **변경**: `prompts/moderate_input.system.jinja2`(reasons/suggestions를 `{{ language_name }}`로 작성 지시), `services/llm.py call_moderation`(language_name=language_display_name 전달), `services/orchestrator.py:359`(한국어 접두어 제거 → reasons 원문만), `screens/loading_screen.dart`(신규 순수헬퍼 `composeJobErrorText` — SAFETY_INPUT은 로컬라이즈 접두어+서버 reasons, 그 외 원문), arb ko/en/ja `loadingSafetyBlockedPrefix` + gen-l10n, 테스트 4(백엔드 i18n·orchestrator 2 + 모바일 3).
- **G32 준수**: 코드 기반 접두어 l10n만 — ErrorInfo.suggestions 전체 배선은 defer(계약 무변경).
- **red→green**: (1) 프롬프트 렌더에 'English' 부재 → 존재, (2) SafetyError.message에 '입력이 안전하지 않습니다' 부재+reasons 존재, (3) 모바일 SAFETY_INPUT 접두어 조합.
- **M28 mock 충돌 후속수정**: M28이 캐릭터 시트 프롬프트에 넣은 '이미지 생성 AI' 문구가 `_call_mock` 라우터(llm.py:205 이미지 분기 트리거)와 충돌 → 캐릭터 시트 요청이 이미지 JSON 반환 → CharacterSheet 검증 실패(골든 4건). 문구를 '그림 생성 모델'로 리워드해 해소(M28 의도·테스트 불변). 골든 하니스가 느려 M28 단독 게이트에선 미검출 → 전체 스위트에서 포착.

### H24 — 출력 안전성 검사가 ja/zh/es에서 no-op(fail-open) → 신규 언어 3종 아동 안전망 부재
- **변경**: `services/llm.py`(신규 `call_output_moderation(text, language)` — LLM 출력 모더레이션, mock 기본 safe), `prompts/moderate_output.system.jinja2`·`moderate_output.user.jinja2`(신규), `services/orchestrator.py moderate_output`(`_KEYWORD_COVERED_LANGUAGES={ko,en}`, 커버리지 밖 언어는 fail-open 대신 LLM 폴백), 테스트 5(ja/zh/es 차단 파라미터 3 + ja safe 통과 + ko/en 폴백 미호출).
- **G17 준수**: LLM 출력 모더레이션 폴백(커버리지 우선). ko/en은 기존 키워드망 유지(비용·지연 회피, 폴백 미호출을 테스트로 고정).
- **red→green**: 수정 전 ja/zh/es는 moderate_output가 무조건 True → 3건 FAIL. 폴백 배선 후 GREEN(골든 ja/zh/es도 mock safe로 통과).
- **⚠ G17 이미지 안전검사 '병행 배선' 부분 편차(CTO 재확인 요망)**: 현 아키텍처는 생성 이미지의 safety 신호가 provider 요청 파라미터(fal `enable_safety_checker`) 외 반환 경로로 없음. Python 측 always-safe 훅은 '검사한 척'하는 안전 연극(CLAUDE.md fail-open 클래스 위반)이라 배선하지 않고, moderate_output에 명시 주석 + 본 로그로 보고. 실질 배선(vision 모더레이션/nsfw 플래그 패스스루)은 이미지 생성 반환 타입 변경 수반 → 별도 스코프. **텍스트 안전 공백(핵심 아동 안전 리스크)은 완전 해소.**

### H1 — 운영 TTS/STT mock 기본값 + readiness 미검사 → 무음 오디오·가짜 발음점수 서빙
- **변경**: `core/config.py`(신규 `audio_feature_enabled: bool=False`, G9), `services/tts.py`·`services/stt.py`(provider **lazy 해석** — 임포트 싱글톤 부팅 크래시 방지[핸드오프 B2] + `_get_provider`가 미지/운영-mock에 raise), `main.py`(`_audio_readiness_issues` + audio_feature_enabled 게이트 하 tts/stt 라이브 검사), `infra/docker-compose.prod.yml`(`:-mock` 기본값 제거 → `${TTS_PROVIDER}` + STT_PROVIDER·AUDIO_FEATURE_ENABLED 패스스루), `.env.example`(오디오/STT 섹션), `scripts/check-env.sh`(STT 추가 + 조건부 게이트 주석), `docs/DEPLOYMENT.md`(오디오 규칙), 신규 `tests/test_provider_readiness.py`(9).
- **G9 준수**: 오디오 GA 기본 비활성 → readiness는 audio_feature_enabled=True일 때만 tts/stt 게이트. **핸드오프 B2 준수**: 생성자 raise 금지(lazy) — 부팅 안전 테스트 포함.
- **⚠ 편차(보고)**: fix step5 'TTS_PROVIDER 무조건 운영 필수 격상'은 G9(오디오 기본 비활성)와 충돌 → 무조건 하드게이트 대신 조건부(AUDIO_FEATURE_ENABLED=true 시 필수)로 구현. 런타임 /health/ready가 최종 게이트(규범 결정>fix_steps).
- **red→green**: 미지/운영-mock provider 해석 raise(수정 전 Mock 폴백) + audio ON readiness 503(tts/stt_provider_not_live) + audio OFF 게이트 스킵.

### H3 — 스토리 5종 vs 오디오·발음 ko/en 전용 → ja/zh/es 책이 한국어 보이스로 오합성·영구 캐시
- **변경(백엔드)**: `services/tts.py`(`TTS_LANGUAGE_CODES`/`TTS_DEFAULT_VOICES`/`SUPPORTED_AUDIO_LANGUAGES` + `resolve_tts_language` — ko/en 하드코딩 제거, 미지원 언어 raise), `services/stt.py`(`STT_LANGUAGE_CODES`/`SUPPORTED_STT_LANGUAGES` + Google 코드 매핑·OpenAI 확장), `routers/books.py`(`_generate_audio_pages` 책 언어 기반 슬롯·MA5 교차오염 제거 / `get_page_audio` pattern `^(ko|en|ja|zh|es)$`·'ko' 강제 제거·ja/zh/es 기본 슬롯 캐시·저장·미지원 ValidationError), `routers/pronunciation.py`(허용 언어 5종).
- **변경(모바일)**: `models/job_status.dart`(BookResult.language 파싱·직렬화), `screens/viewer_screen.dart`(책 언어로 `_selectedLanguage` 최초 초기화 → 오디오 요청이 책 언어), `screens/pronunciation_practice_screen.dart`(language:'ko' 고정 → widget.language), `main.dart`(라우트 language 인자 전달).
- **계약**: openapi.json audio language pattern 5종으로 재export(자동 생성, diff 1줄). 계약 신선도 통과.
- **G9 준수**: fail-open(오합성·무음·422) 제거 + 미지원 명시 차단. 보이스 실프로비저닝은 audio_feature_enabled(H1) 하에서만 활성.
- **MA5(핸드오프) 준수**: 비 ko/en 언어는 audio_url_ko 오염 없이 기본 슬롯(page.audio_url)에 저장·조회 → 교차 오염·매요청 재합성 제거.
- **red→green**: tts/stt 매핑 테스트는 ImportError RED 확인. books_audio 라우터 3 + 모바일 1은 구현 후 GREEN 검증(구 코드 분기상 language='ko' 강제/파싱 부재로 RED가 자명 — 데모 생략).
- 테스트: 백엔드 +6(tts/stt 매핑 3, books_audio 3), 모바일 +1(BookResult.language).

### H15 — JIT 사진 동의: getConsent 실패 시 빈 맵 echo → 기존 필수 동의 파괴
- **변경**: `core/photo_consent.dart`(getConsent catch에서 빈 맵 폴백 제거 → fail-closed 중단 + 재시도 스낵바), arb `photoConsentLoadFailed` ko/en/ja.
- **G21 준수**: fail-closed 최소 수정(부분 갱신 API는 후속). grant_consent supersede 시맨틱 불변(클라 방어만).
- **red→green**: 구 코드는 throw 시 grantConsent(privacy:false,dataProcessing:false) 전송으로 필수 동의 revoke → 신규 `_ThrowingApi` 테스트가 grantCalled=false·result=false 요구.

### H16 — JIT 사진 동의 다이얼로그(PIPA 법정 고지) 한국어 하드코딩 → en/ja 읽을 수 없는 동의
- **변경**: `core/photo_consent.dart`(제목·PIPA 5요소 본문·버튼을 l10n으로 — 기존 `consentPhotoOptionalTitle`/`consentPhotoDisclosure` 재사용 + 신규 `photoConsentAgree`/`photoConsentCancel`), 테스트 로캘화(_hostLocalized) + en 검증.
- **red→green**: 구 다이얼로그는 const 한국어 → en 로캘 테스트가 'Recipient'/'Agree' findsOneWidget·한국어 부재 요구. 기존 ko 3케이스 finder 갱신(회귀 유지).

### H17 — 폴링 4분 예산이 서버 SLA 10분과 불일치 + 동기 엔드포인트 30초 타임아웃 → 허위 실패·중복 생성·한국어 에러 (모바일 코어)
- **변경**: `providers.dart`(maxAttempts(≈4분) 제거 → elapsed 10분 단일 예산 + 순수 헬퍼 `jobPollingBudgetExceeded`/`kJobPollingHardTimeout`), `loading_screen.dart`(TimeoutException 타입 감지 → errorCode='TIMEOUT'·`composeJobErrorText`가 `loadingTimeoutMessage`로 매핑(원문 미노출) + 타임아웃 시 주 버튼을 신규 생성 대신 현재 잡 재조회로), `api_client.dart`(retell·from-photo·from-drawing·getPageAudioUrl에 per-call `receiveTimeout=180s` — orphan 감소), arb `loadingTimeoutMessage` ko/en/ja, 테스트 +3(예산 헬퍼·TIMEOUT l10n 3로캘).
- **G19 준수(부분)**: receiveTimeout 상향 + 폴링 예산 정합 + 타임아웃 재조회(중복 생성 주경로 제거). **미완(H17 잔여)**: retell/from-photo/from-drawing **서버측 멱등 수용 + 클라 시도 멱등키 + 계약(X-Idempotency-Key)** — MA7에 따라 서버 수용 후 계약 문서화. defense-in-depth(receiveTimeout-orphan)용, 별도 서브커밋으로 처리 예정.
- **red→green**: 예산 헬퍼(4분·9:59 미초과, 10:01 초과) + TIMEOUT→로컬라이즈(TimeoutException 원문 부재). 구 코드 대비 RED 자명(4분 death·error.toString 노출).

### M12 — 재생성·리텔·인페인트 경로 결함 묶음(입력/출력 모더레이션 우회 + no-op done 위장)
- **변경(백엔드)**: `orchestrator.py`(신규 순수 헬퍼 `_moderate_text` — moderate_output 키워드 검사 추출·재사용; `regenerate_page`에 feedback 입력 모더레이션(SAFETY_INPUT)·draft 부재 시 명시 실패(no-op done 위장 제거)·revised_text 빈값 가드·출력 모더레이션(SAFETY_OUTPUT); `inpaint_page`에 region_prompt 입력 모더레이션), `routers/books.py`(retell 결과 출력 모더레이션 → 저장 전 SAFETY_OUTPUT 차단; regen/inpaint 잡 wrapper가 StoryBookError.code 보존 — UNKNOWN 뭉갬 제거), `models/dto.py`(RegeneratePageRequest `model_validator` — mode=text/both는 feedback 필수 422).
- **M31 선행 활용**: call_text_rewrite는 이미 parse_json_response(RewriteResult) 경유(M31) — 펜스 처리·검증 완료. M12는 그 위에 빈값 가드·출력 모더레이션 추가.
- **규범(B/G 게이트) 정합**: 최초 생성에만 있던 입력(B)·출력(G) 안전 게이트를 재생성·리텔·인페인트 3경로에 파리티 배선. 규범 결정 불요(코드가 스펙 위반한 버그).
- **red→green**: `_moderate_text`(정상 True·금칙 False), RegeneratePageRequest 검증(text/both+feedback 없음 거부), retell 금칙 결과 422 SAFETY_OUTPUT+미저장, regenerate 금칙 feedback SAFETY_INPUT+page 불변, draft 부재 명시 실패+미커밋. 전건 구현 전 RED 확인(retell 200 데모).
- 계약: RegeneratePageRequest 필드 불변(validator는 스키마 미노출) → openapi 무변경(재export diff 0).
- **회귀 1건 정정(테스트 전용)**: `test_regenerate_before_completion_uses_validation_error`가 mode=text·feedback 없이 '생성 미완료 400' 도메인 체크에 도달하던 것을 새 validator가 422로 선차단 → 테스트에 유효 feedback 추가(의도 보존). 프로덕션 회귀 아님.

### M15 — 사용자 노출 한국어/₩ 하드코딩 제거 (플랜 표시 슬라이스)
- **변경**: `screens/credits_screen.dart`(신규 순수헬퍼 `localizedPlanName`/`localizedPlanFeatures` — 서버 plan `id`(free/basic/premium 안정키)로 이름·features를 l10n, 미지 id는 서버값 폴백), arb `planFree/Basic/Premium`·`planFeaturesFree/Basic/Premium`('|' 구분) ko/en/ja, 테스트 +2(로케일별 이름·features). 백엔드 plan `id`는 이미 안정키 응답(credits.py:120) — 무변경.
- **G7 준수(부분)**: 표시는 로케일 그대로. **미완(M15 잔여, 별도 서브파트)**: (1) 구독 가격 스토어 `ProductDetails.localizedPrice` 배선(현재 ₩ 서버가 여전 — IAP 상품 로딩 라이프사이클 필요), (2) 전 화면 에러 code→l10n 매핑 계층(api_error.dart), (3) 앱 타이틀 onGenerateTitle·알림 채널명·providers 폴백·iap/kakao 문자열 스윕. **이유**: M15는 다파일 광역 l10n 스윕이라 한 번에 완결 시 누락 식별 불가 위험 — 가장 가시적·결정적 슬라이스(플랜명)를 완결하고 잔여를 명시 분리.
- **red→green**: en/ja에서 '베이직'·한국어 features 미노출(로케일 값 반환), 미지 id 폴백.

### M15 (2) — 전 화면 에러 code→l10n 매핑 계층
- **변경**: `core/api_error.dart`(신규 `localizedMessage(AppLocalizations)` — 네트워크/HTTP 카테고리 코드(CONNECTION/TIMEOUT/VALIDATION/RATE_LIMIT/SERVICE_UNAVAILABLE/UNAUTHORIZED/FORBIDDEN/INTERNAL/NETWORK/CANCELLED)를 l10n으로; NOT_FOUND/BAD_REQUEST/PAYMENT_REQUIRED는 서버 구체 메시지 유지), arb error* 10키 ko/en/ja, 소비부 `pod_order_screen`(2)·`library_screen`·`character_source_sheet`(_errorMessage에 l 주입)를 localizedMessage로 이관, 테스트 +2(api_error_test).
- **판단**: PAYMENT_REQUIRED(402)는 무료플랜 한도 등 서버가 구체 사유를 담아 일반화 시 의미 소실 → 매핑 제외, 서버측 402 로컬라이즈는 M15 서버 잔여로 분리. api_client.dart의 DioException.message 폴백은 비표시 경로라 유지.
- **red→green**: en/ja localizedMessage가 한국어 미포함(errorConnection 등), 서버메시지 코드는 message 보존.

### M15 (3) — 앱 타이틀 로케일화
- **변경**: `main.dart`(`title: 'AI 동화책'` 하드코딩 → `onGenerateTitle: (ctx)=>l.appTitle` — 작업전환기/웹탭 타이틀 로케일별). appTitle 키는 3로케일 기존 존재.

## Wave 4 소계 (부분, 지속)
- 완료 19 + H17·M15(3슬라이스): …·H15·H16·M12 + H17(모바일코어)·M15(플랜표시+에러l10n계층+앱타이틀). 백엔드 533 pass·ruff·단일 head·계약 신선도·골든 통과. 모바일 **218 pass**. **W4 잔여: M15 저가치 롱테일(가격 스토어배선·서버402 l10n·알림채널·providers/iap/kakao 문자열) + H17 서버멱등 서브파트.**
- money 마이그레이션 실DB(PG): H2(user_settings.timezone additive)·H14(daily_stories dedup+unique) 리허설 완료.
- 커밋: W4-part1(d31f50b)·part2(4d4ee1e)·part3(7da0757)·part4(31d87e1)·part5(예정).
- **남은 W4 6**: 오디오 i18n 클러스터 H1·H3·H24·M28·M29·M15(G9 기능플래그 결정 연동, M15는 대형 모바일 l10n) + 파이프라인 M12 + 모바일 법정 H15·H16·H17.

---

---

## Wave 5 (진행)

### M30 — dup of H3 (클로즈, 코드 없음)
- get_page_audio + tts 언어매핑은 H3에서 완결(pattern 5종·'ko' 강제 제거·기본슬롯·ja-JP 매핑·`test_get_page_audio_ja_uses_base_slot_and_ja_voice`). MA5 지침대로 중복 편집 없이 dup 클로즈.

### H26 — dup of H14 (클로즈, 코드 없음)
- daily_stories.date UNIQUE + get_today_story IntegrityError rollback·재조회·방어적 first()는 H14에서 완결(db.py:365 `uq_daily_stories_date`, streak.py:474/511/521). dup 클로즈.

### H7 — series FK 미처리로 erasure 500 (HIGH)
- **변경**: `routers/users.py`(삭제 순서 Book→Series로 교정 — books.series_id FK 위반 제거, Series는 여전히 Character 앞), `routers/characters.py`(delete 전 `update(Series).where(character_id==).values(character_id=None)` — 단방향 FK nullify), `tests/test_data_deletion_fk.py`(+2, SQLite FK 강제). **선택 마이그레이션(ON DELETE SET NULL)은 앱레벨 수정으로 DoD 충족돼 defer.**
- **red→green**: 시리즈 사용 계정 삭제 200, 시리즈 만든 캐릭터 삭제 200+series.character_id null(구 코드 IntegrityError 500).

### H11 — PDF SSRF 허용목록 s3_public_url 호스트 누락 (HIGH)
- **변경**: `services/pdf.py _is_url_allowed`(allowed에 `s3_public_host` 추가 — storage.py 가드와 정합; 저장 이미지 URL이 s3_public_url/{key}라 R2 공개도메인/CDN 구성에서 삽화 전무 PDF였음), `tests/test_services.py`(+2: 공개호스트 허용·SSRF 차단 회귀).
- **red→green**: cdn.example.com 허용(구 코드 DNS 실패 fail-closed False), 169.254 메타데이터·file:// 여전 차단.

### H8 — 계정 삭제 스토리지 파기 실패가 이중 삼킴 → 항상 'success·실패0' (HIGH, G24 최소)
- **변경**: `services/storage.py`(신규 `_delete_prefix_keys(prefix)->list[str]` — ClientError·per-key Errors 미삼킴·list_objects_v2 페이지네이션; `delete_book_files`/`delete_prefix`가 실패키 목록 반환[기존 int 반환은 미사용이라 안전]), `routers/users.py delete_my_data`(실패키 수집 → `status='partial'` + 식별 로그), `routers/consent.py`(revoke 실패키 로그), 테스트 +2.
- **G24 준수**: 응답 status=partial + 식별 로그(최소). 영속 장부·재시도 배치는 규제 압박 시 승격(defer).
- **H7 blast-radius 정정(테스트 전용)**: `test_delete_character_rolls_back_on_commit_failure`가 H7의 update(Series) 추가로 execute 2회가 되어 fake 세션 결과 1개로 IndexError → 결과 2개로 정정.
- **red→green**: delete_book_files가 delete_objects Errors를 실패키로 반환, 계정삭제 status=partial(구 코드 success·0).

### M10 — 모델↔마이그레이션 드리프트: retelling_source_book_id self-FK 부재 + purge 미해제
- **변경**: `models/db.py`(FK에 `ondelete='SET NULL'` 명시), `services/data_deletion.py purge_book_children`(retelling_source_book_id nullify 추가; Book import), 신규 마이그레이션 `f6a7b8c9d0e1`(고아 정리 UPDATE 선행 → PG 조건부 `fk_books_retelling_source_book_id_books` ON DELETE SET NULL, SQLite는 create_all이 보유), `tests/test_retell.py`(+1).
- **PG 리허설 실측**: 전체 체인 upgrade 성공·단일 head, **ON DELETE SET NULL 동작 확인**(orig 삭제→variant.retelling_source_book_id NULL), **`alembic check`='No new upgrade operations'(드리프트 해소, diff-0)**.
- **red→green**: 원본 삭제 시 변형본 링크 null화(구 코드 dangling 포인터·FK 부착 시 삭제 실패).

### H23 — 페이지 재생성(call_text_rewrite) 언어 미전달 → 비한국어 책 한국어 회귀 + 이중언어 비동기화 (HIGH)
- **변경**: `services/llm.py call_text_rewrite`(language·language_name 전달 — generate_story 패턴), `prompts/rewrite_page_text.system.jinja2`(revised_text 언어 규칙 + user_prompt language 필드), `services/orchestrator.py regenerate_page`(책 언어 컬럼 갱신 text_ko/text_en + 반대 언어 컬럼·전 오디오 무효화 None), 테스트 +2.
- **G18 준수**: 반대 언어 컬럼·오디오는 무효화(None)만(즉시 재번역·재TTS 안 함, 최소안). M31의 RewriteResult 반환 타입과 정합(이미 .revised_text).
- **red→green**: en spec 프롬프트에 'English' 지시·user에 'language: en', en 재작성 후 text_en 갱신·text_ko/오디오 무효화(구 코드 language 미전달·컬럼 미동기).

### M32 — 오케스트레이터 current_step 한국어 하드코딩 → 전 로케일 한국어 진행문구 (MEDIUM)
- **변경**: `services/orchestrator.py`(run_step step_name 8종·mark_job_done·generate_images를 안정 영문 키로: normalize/moderate_input/generate_story/generate_character_sheet/generate_image_prompts/moderate_output/learning_assets/package/generate_images/done; 페이지 n/total은 progress가 표현 → 키에서 제거), `models/db.py`(current_step default 'queued'), `routers/streak.py`·`routers/books.py`(생성 초기 default 'queued'), `screens/loading_screen.dart`(_getStepDescription 맵에 queued/preparing/learning_assets/done 추가 — `?? step` 폴백이 한국어 미노출), arb `loadingStepLearningAssets` ko/en/ja, 테스트(백엔드 소스 가드 +1, 모바일 l10n +1).
- **G33 준수**: 이미지 카운트는 progress 의존(별도 step_detail 필드 미도입, 최소안).
- **바운드**: 재생성/인페인트/시리즈 전용 진행문구(books.py 402/820/1034 등)는 메인 로딩 맵 밖 별도 폴링 서브플로우 — 이번 스코프에서 제외(후속). 메인 생성 파이프라인 + 공유 default는 전건 키화.
- **red→green**: orchestrator 소스에 한국어 진행문구 부재·안정 키 존재. 계약(current_step 자유문자열) 무변경.

### M25 — 인페인트 _waitForJob이 잡 실패·타임아웃을 성공으로 처리 (MEDIUM)
- **변경**: `screens/inpaint_screen.dart`(신규 `enum _InpaintOutcome {completed,failed,timeout}`; `_waitForJob`가 isComplete→completed·isFailed→failed·예산 소진→timeout 반환, 폴링 예산 40초→3분(이미지 90초×재시도 정합); `_apply`가 결과 분기 — failed/timeout은 pop(true) 안 하고 스낵바), arb `inpaintApplyingRefreshLater` ko/en/ja.
- **red→green**: 구 코드는 failed/timeout에서 무신호 return 후 pop(true)='성공' 위장 → 분기로 오분류 제거. analyze 0. (전체 _apply 위젯 e2e는 캔버스 제스처+1초 폴링 타이밍으로 브리틀 — 구조적 enum 분기로 대체, 후속 e2e 여지.)

### M24 — characters_screen 사진 업로드에 보호자 age gate 누락 (MEDIUM)
- **변경**: `widgets/age_gate_dialog.dart`(신규 공용 헬퍼 `ensureAgeGate` — 세션 검증 시 통과, 아니면 다이얼로그), `screens/characters_screen.dart _pickImage`(사진 선택 전 게이트 추가), `widgets/character_source_sheet.dart _usePhoto`(인라인 게이트 → 공용 헬퍼로 치환 — 두 진입점 일치), 신규 테스트 2(검증 시 통과·미검증 시 다이얼로그).
- **red→green**: 미검증 세션에서 characters_screen 사진 경로가 게이트를 강제(구 코드 게이트 없이 JIT 동의로 직행). analyze 0.

### M20 — 규범 스펙 vs 코드 정합성 묶음 (부분: Replicate 폴링)
- **변경**: `services/image.py _generate_replicate`(폴링 상한 `range(60)` → `range(max(1,int(settings.image_timeout)))` — 60~90초 정상 완료가 IMAGE_TIMEOUT으로 조기 실패하던 스펙 불일치 해소), 테스트 +1(소스: range(60) 부재·image_timeout 파생).
- **G16 준수(부분)**: 진행률·타임아웃 표는 코드 실측으로 스펙 개정 방침. UNKNOWN 뭉갬=H9 완료(중복 금지), ja/zh/es 텍스트 안전=H24 완료(참조만).
- **미완(M20 잔여, 별도 구현)**: (1) SAFETY_OUTPUT 재시도 2× 구현(G16), (2) G단계 출력 이미지 vision 모더레이션 배선(G16/G17 — H24에서 안전연극 회피로 defer한 실질 배선), (3) CLAUDE.md 진행률/타임아웃 표를 코드 실측으로 개정(문서). 모두 기능/문서 규모 작업.
- **red→green**: Replicate 폴링이 image_timeout 파생(구 코드 60 하드코딩).

## Wave 5 소계 (진행)
- 완료 9: (dup 클로즈) M30·H26 + H7·H11·H8·M10·H23·M32·M25·M24. 백엔드 **543 pass**·ruff·단일 head `f6a7b8c9d0e1`·M10 PG 리허설(SET NULL·check diff-0)·계약 신선도. 모바일 **221 pass**·analyze 0.
- **W5 잔여**: M27·H25(둘 다 규모 L 카피라이팅 — 프리셋/테마 로케일 텍스트 대량), M20(정합성 묶음, 상당수 H9/H24로 이미 처리), M22(오늘 book_id, ⚠결정), M23(Celery 재전달 멱등), M11(시리즈 Celery), L7·L9·L12(스트릭/성장 정합), N1(이미지 삭제 추적).

---

## Wave 5 병렬 배치 (4 file-disjoint 티켓, 워크트리 에이전트 → HEAD 통합)

**중요**: 워크트리가 구 베이스(119a2b9 = PR#57 머지, 내 세션 커밋 전부 미포함)에서 잘려, 4 에이전트가 스테일 베이스로 구현. 직접 cherry-pick은 (a) 스테일 openapi가 내 계약 변경 리버트, (b) N1이 H8 중복, (c) orchestrator 9커밋 갭 충돌 위험. → 통합 전략: M23/M27은 auto-merge(무충돌 검증), M22는 hand-port(H14/H2 반영본 get_today_story 위), N1은 H8 보유 HEAD 위 재구현(역산삭제 최소안). openapi는 통합 앱에서 재export.

### M23 — Celery 재전달 비멱등 → job_id unique 충돌로 복구가 확정 실패 (커밋 25222b3)
- `services/tasks.py`: 순수 헬퍼 `is_redelivery_noop` + `_get_job_status_async`, generate_book_task 초입 terminal(done/failed) 조기 return, 중복 insert IntegrityError를 already-claimed no-op으로 흡수(재전달 실패 루프 차단), 진짜 에러는 mark_failed+re-raise 유지. 테스트 +7(test_tasks.py). auto-merge 무충돌·7 pass.

### M27 — 캐릭터 프리셋 한국어 전용 서빙 → en/ja 노출 (커밋 a5ae27a)
- G31: 표시명(name/appearance/clothing) 로케일화, master_description 영어 고정. `get_preset_localized`(ko 폴백), GET /presets·from-preset에 language 파라미터, 모바일 앱 로케일 전달. auto-merge가 H7(characters.py delete_character nullify) 보존 확인, openapi 통합 재export(스테일 리버트 없음). 백엔드 10 + 회귀 59 pass, 모바일 221 pass.

### M22 — 오늘의 동화 book_id 항상 null → 재탭 크레딧 반복 (커밋 65c4e96)
- G12: 익명 유지 + X-User-Key 있을 때만 book_id 채움('오늘의 동화 생성' 크레딧 마커 done Job). 전역 DailyStory에 미기록(오귀속 없음). H14 레이스·H2 tz 반영본 get_today_story 위에 hand-port. 계약 optional 헤더. 10 pass.

### N1 — 파이프라인 이미지 삭제 추적성 (커밋 434317b)
- 역산 삭제(⚠결정 최소안): `collect_book_image_keys`가 image_url에서 S3 키 역산(행 삭제 전), `storage.delete_keys`(청크·실패키 반환), users.delete_my_data가 H8 status=partial 계약에 합산. orchestrator/image_storage_scope 미사용(에이전트 스테일 구현 폐기), H8 보유 HEAD 위 재구현. 11 pass.

### L9 — 프로필 경유 읽기가 계정 DailyStreak 미갱신 → books_read>0인데 streak=0 모순 (LOW)
- **변경**: `services/streak.py _get_profile_streak_info`(profile_id Optional=None — None이면 user_key 전체 ReadingLog로 계정 스트릭 재계산), `services/growth.py`(계정 분기가 daily_streaks 직접 조회 대신 ReadingLog 재계산 사용; DailyStreak import 제거), 테스트 +1.
- **G33 재량 = ReadingLog 재계산 통일**(옵션1, 더 일관적). get_streak_info는 불변(블라스트 반경 한정).
- **red→green**: 프로필 경유 3일 읽기 후 계정 GET /growth의 current_streak≥1·total_reading_days≥3(구 코드 streak=0 모순).

### L7 — 스트릭 캘린더가 '상대 윈도우'로 조회 → 2~3개월 이전 달 항상 빈 캘린더 (LOW)
- **변경**: `core/utils.py`(신규 `local_month_range_utc(year, month, tz)` — 임의 달 절대 UTC 경계, DST 안전), `services/streak.py`(신규 `get_calendar_month` — 요청 월 절대 경계로 ReadingLog 직접 조회·로컬 일별 distinct book 집계), `routers/streak.py get_streak_calendar`(상대 윈도우 get_reading_history 제거 → get_calendar_month 사용), 신규 테스트 3.
- **red→green**: 90일 전 읽기가 그 달 캘린더에 read:true(구 코드 상대 윈도우 밖이라 전부 false·0, growth와 모순), 빈 달 오탐 없음, 월 경계 반열림.

### L12 — 이미 지급된 마일스톤 보상 재노출 + growth 프로필 소유권 미검증 (LOW)
- **(a) G23=계정 1회 지급**: `services/streak.py _grant_milestone_rewards`가 각 마일스톤에 실지급 여부(`granted`)를 표기하고, 이미 지급된 보상은 `reward=None`으로 비워 '미지급 축하' 재노출 제거(둘째 프로필 재도달 등). 단위 테스트 +1.
- **(b) 소유권 검증**: `core/dependencies.py`에 공용 `validate_profile_ownership` 승격, growth 3 엔드포인트(get_growth·peers·answers)에 적용 — 삭제/타인 profile_id면 400(ValidationError)으로 0-리포트·dangling QuizAnswer·age_band 폴백 우회 차단. 테스트 +2.
- **red→green**: 이미 지급 마일스톤 granted:false·reward:null / 무효 profile_id 400+QuizAnswer 미저장.

## Wave 5 소계 (진행, L9/L7/L12 추가)
- 완료: (dup) M30·H26 + H7·H11·H8·M10·H23·M32·M25·M24·M20(부분)·M23·M27·M22·N1·L9·L7·L12. 백엔드 pass·ruff·단일 head. **W5 잔여: H25(규모 L 카피라이팅)·M11(시리즈 Celery)·M20 잔여(SAFETY_OUTPUT 재시도·vision 모더레이션).**

### M11 — 시리즈 생성이 USE_CELERY에서도 API 프로세스 BackgroundTasks 실행 → 재시작 유실·API 지연 (MEDIUM)
- **변경**: `services/tasks.py`(신규 `generate_series_task` + `_run_series_generation_async` — 직렬화 가능한 id 인자로 character/prev_book DB 재조회 후 start_series_generation 호출; M23 재전달 멱등 가드 미러), `routers/books.py create_series_next`(create_book 대칭 `if use_celery: generate_series_task.delay(...ids)` else add_task 분기), 신규 테스트 2.
- **orchestrator 무변경**: 태스크가 재조회 후 기존 start_series_generation(ORM 인자)을 호출 — 시그니처 불변(블라스트 반경 최소).
- **red→green**: use_celery=True에서 delay가 id 인자로 1회 호출(add_task 아님), enqueue 실패 시 예외 없이 처리.

### M20 (2) — SAFETY_OUTPUT 재시도 2회 (G16)
- **변경**: `services/orchestrator.py start_book_generation`(스토리 생성 직후 출력 텍스트 안전검사 + unsafe 시 최대 2회 재생성 루프 — 이미지 비용 '전'에 검사; 2회 후에도 unsafe면 SAFETY_OUTPUT 실패). 이미지 뒤 중복 G 텍스트 게이트 제거(재시도 루프가 정본; ja/zh/es LLM 이중호출 방지). 테스트 +2.
- **G16 준수**: 스펙 '재시도 2회' 구현(구 코드는 SafetyError 즉시 종료=0회). 골든·통합·e2e 42 pass로 파이프라인 무손상 확인.
- **M20 잔여**: vision 이미지 모더레이션(provider safety 신호 부재로 H24/M20 별도 스코프), CLAUDE.md 진행률/타임아웃 표를 코드 실측으로 개정(문서).
- **red→green**: 계속 unsafe면 generate_story 3회(1+2재시도)·SAFETY_OUTPUT; 2번째 safe면 통과.

### H25 (mobile) — 오늘의 동화 테마/토픽 로케일 표시
- **변경**: `providers.dart HomeStreakSnapshot`(todayThemeId/todayTopicId 추가 — 서버 theme/topic_id), `screens/home_screen.dart`(신규 `dailyThemeLabel`/`dailyTopicLabel` 헬퍼 — 안정 id→로케일 arb, 미지 id는 서버 폴백; 오늘의 동화 패널이 로케일 표시), arb `dailyTheme{7}`+`dailyTopic{28}` = 35키 × ko/en/ja(105 엔트리) + gen-l10n, 신규 테스트 1.
- **G11 준수**: 서버 topic_id-only + 모바일 arb 표시. 서버 한국어 theme_name/topic은 폴백 유지.
- **red→green**: en/ja에서 테마/토픽이 로케일 값(한국어 미노출), 미지 id 폴백. 백엔드 575·모바일 222 pass.

## Wave 5 소계 (대부분 완료)
- 완료: (dup)M30·H26 + H7·H11·H8·M10·H23·M32·M25·M24·M20(폴링+SAFETY_OUTPUT재시도)·M23·M27·M22·N1·L9·L7·L12·M11·H25. **W5 잔여(비로직/문서/미배선)**: M20 vision 이미지 모더레이션(provider safety 신호 부재로 별도 스코프)·CLAUDE.md 진행률/타임아웃 표 개정(문서), M15 롱테일(스토어 가격·알림채널·providers/iap/kakao 문자열·서버402 l10n).

---

## Wave 6 (인프라·배포·CI·네이티브)

### M9 — /health/detailed·/ready 무인증 노출 → 내부 메트릭·config·missing_keys 정찰 (MEDIUM)
- **변경**: `main.py`(`detailed_health_check`에 X-Admin-Key hmac.compare_digest 인증 — 미설정/불일치 403; `_build_readiness_payload`에 `expose_missing_keys` 게이트 — 공개 /ready는 provider_keys boolean만, 상세 missing_keys는 detailed에만), `infra/nginx/nginx.conf`(/health prefix → 정확 매칭 + rate limit; /health/detailed 공개 엣지 deny), 테스트 +2.
- **red→green**: detailed 무인증 403·정상키 200, /ready 503이되 'iap_webhook_secret_missing'·missing_keys 미노출.

### CI 게이트 클러스터 — M2·M3·M4·M5·M6·M7·M1·L1·L3·L4 (한 커밋, 상호의존)
- **TDD 가드 신규**: `apps/api/tests/test_ci_workflow_gates.py`(PyYAML 6.0.3, 15 테스트) — ci.yml/run_live_e2e.sh 구조 불변식을 잠근다. 12 RED→구현→15 GREEN. CI 런타임은 파이프라인에서만 검증 가능하므로 가드는 '게이트 조용한 무력화' 회귀 방지용.
- **M3/M4/M5(pipefail)**: pytest·flutter analyze·flutter test·integration_test의 `... 2>&1 | tee` 파이프에 `set -o pipefail` 추가 — tee가 좌측(테스트) 종료코드를 삼켜 실패가 green으로 통과하던 마스킹 제거.
- **M2(스캔 게이트 실질화)**: safety 스텝 `|| echo "..."` 제거(실패를 성공으로 위장 금지). Trivy repo(fs) 스캔 `exit-code 0→1`, severity `CRITICAL,HIGH→CRITICAL`. **정책=G30: CRITICAL blocking**(아동 대상), HIGH는 image SARIF로 가시화만.
- **M7(스캔 순서 + worker)**: build 잡 리팩터 — api/worker 이미지를 `push:false·load:true`로 먼저 빌드 → **push 이전** blocking 스캔(exit-code 1, api+worker 둘 다) → 통과 시에만 별도 `docker push` 스텝으로 :latest·:sha 게시. worker 이미지 무스캔 해소. SARIF 업로드 2건 `if: always()`. cache-from/to gha 유지.
- **M6(핀 고정)**: `aquasecurity/trivy-action@master`(3곳) → `@0.24.0` 버전 태그 핀.
- **M1(배포 직렬화)**: 워크플로 레벨 `cancel-in-progress: true` → `${{ github.event_name == 'pull_request' }}`(PR 중복만 취소, main push 런 보존). build·deploy 잡에 `concurrency: {group, cancel-in-progress:false}` — 진행 중 배포/빌드 취소 금지·직렬화(반쪽 태그·부분배포 방치 방지).
- **L1(스텝명 정직화)**: 'Check environment contracts'(파일 존재 검사만) → 'Check env example files exist'. 저등급이라 정직화 옵션 채택(실체화 아님).
- **L3(스테일 버전 핀)**: ci.yml Live E2E `APP_VERSION: "0.3.2"` env + run_live_e2e.sh:26 `${APP_VERSION:-0.3.2}` 제거 → config.py 기본 1.0.0 단일 정본으로 E2E 구동.
- **L4(리포/이미지 스큐)**: deploy SSH 스크립트 `git checkout main; git pull --ff-only origin main` → `git fetch origin; git checkout --detach "$GITHUB_SHA"` — 서버 리포(compose/nginx/deploy.sh/smoke.sh)를 빌드된 이미지와 동일 커밋에 고정.
- **문서**: DEPLOYMENT.md 'Release gate hardening' 절 추가(CVE 정책 CRITICAL blocking·스캔 선행·배포 직렬화·detached HEAD 운영 노트 — M1/M2/L4 DoD).
- **검증 한계**: ci.yml 변경의 실런타임(스캔이 실제 red, 배포 큐잉)은 파이프라인에서만 확인 가능. 로컬 가드는 YAML 구조 불변식만 잠금. actionlint 미설치 → PyYAML safe_load 파싱 + 가드 테스트로 대체.

### H22 — prod compose 환경변수 패스스루 드리프트 (HIGH)
- **변경**: `infra/docker-compose.prod.yml`(api·worker: IMAGE_MODEL·STT_API_KEY·GOOGLE_STT_API_KEY·POD 블록[POD_MODE/POD_PROVIDER/PRINTFUL_*] 추가; api 전용: ADMIN_API_KEY·SHARE_BASE_URL), `infra/.env.example`(전면 정리 — IMAGE_MODEL 가이드·STT/POD/IAP 섹션·ADMIN_API_KEY·SHARE_BASE_URL·USE_CELERY 추가; 죽은 변수 SECRET_KEY/ENVIRONMENT/LOG_LEVEL 제거·RATE_LIMIT→RATE_LIMIT_REQUESTS/WINDOW), `scripts/check-env.sh`(PRODUCTION_REQUIRED_VARS에 IAP_VERIFICATION_MODE·IAP_WEBHOOK_SECRET; OPTIONAL에 APPLE_IAP_SHARED_SECRET·GOOGLE_PLAY_PACKAGE_NAME), TDD 신규 `tests/test_deploy_config.py`(5 테스트).
- **⚠ 핸드오프 정정 MI3 준수**: STT_PROVIDER/TTS_PROVIDER는 `:-mock` 폴백 없음(H1이 제거한 fail-open 재도입 금지) — compose는 이미 충족, 가드 테스트로 잠금.
- **G-결정**: 스토어=양쪽이나 자격증명은 어느 쪽도 강제 안 함(OPTIONAL 경고). IAP strict/웹훅 시크릿만 필수. CVE 정책과 별개.
- **실측 검증**: `docker compose --env-file infra/.env.example -f infra/docker-compose.prod.yml config`가 api/worker에 IMAGE_MODEL=dall-e-3·POD_MODE=local 렌더, ADMIN은 api에만. `check-env.sh --mode production --env-file infra/.env.example`가 FAIL(IAP_WEBHOOK_SECRET 등 누락). 4 RED→5 GREEN, ruff 통과.

### M26 — deploy.sh 배포 순서·롤백·볼륨 정리 (MEDIUM)
- **변경**: `scripts/deploy.sh`(deploy case 순서 `pull→stop→up→migrate` → `capture→pull→migrate→up→health||rollback`; stop_services(compose down) 제거 — 다운타임 없음; `capture_running_images`/`rollback` 함수 신설 — health 실패 시 이전 이미지 태그로 `compose up -d` 자동 롤백; cleanup의 `docker volume prune -f` 제거 — DB named volume 전손 차단; print_help 갱신), `docs/DEPLOYMENT.md`(deploy 순서 + **expand-then-contract 마이그레이션 규율** 명문화), TDD 신규 `tests/test_deploy_script.py`(5 정적분석 테스트 + bash -n).
- **G-결정 전제**: migrate-before-up은 구 코드가 신 스키마와 잠깐 공존해도 안전함을 요구 → **expand-then-contract(파괴적 마이그레이션은 다음 릴리스로 분리) 규율 채택**. DEPLOYMENT.md에 팀 계약으로 기록.
- **검증**: `bash -n` 통과, 4 RED→5 GREEN. 실배포 검증은 서버 필요(정적 불변식만 잠금).

### L15 — 인프라 저위험 하드닝 묶음 (LOW)
- **변경**: `infra/docker-compose.prod.yml`(api healthcheck → 의존성 인지형 `/health/ready`[이미지 기본 /health는 무조건 200]; minio ports → `127.0.0.1:` 바인딩), `infra/docker-compose.yml`(dev postgres 16→15-alpine, prod/CI 통일), `infra/nginx/nginx.conf`(client_max_body_size 10M→12M, 앱 사진 10MB+multipart 여유), `apps/api/.dockerignore`(`.env`/`.env.*` 제외 + `!.env.example`), TDD 신규 `tests/test_infra_hardening.py`(5 테스트).
- **worker healthcheck 제외**: worker는 HTTP 서버 아님(uvicorn 아님) → HTTP 프로브 부적합, 스펙대로 api만 배선.
- **dev postgres 하향 주의**: 로컬 볼륨에 16 데이터가 있으면 재생성 필요(데이터 손실 아님, dev 한정) — 커밋 메시지 명시.
- **검증**: 5 RED→GREEN, ruff 통과. `docker compose config`가 api healthcheck를 http://localhost:8000/health/ready로, minio를 127.0.0.1 바인딩으로 렌더 확인.

### M9 사후 정정 — 기존 health/readiness 테스트를 M9 계약에 정합 (regression-0 복구)
- **문제**: M9 커밋(aa5f372)을 2개 신규 테스트만 돌리고 풀 회귀 없이 커밋 → 풀 스위트에서 7건 실패 발견(적대 검토 게이트가 포착). status 로직은 정상(503 유지), 실패는 **의도된 계약 변경의 테스트 미갱신**: (a) /health/detailed가 이제 X-Admin-Key 필수 → test_api 3건이 403, (b) /ready가 missing_keys 상세 은닉 → provider_readiness 2건·iap_hardening 1건이 상세 사유 미검출, (c) detailed에 Header 파라미터 추가로 openapi 계약 stale 1건.
- **변경**: `tests/test_api.py`(detailed 3건에 admin_api_key patch + X-Admin-Key 헤더), `tests/test_provider_readiness.py`·`tests/test_iap_hardening.py`(/ready는 503+provider_keys unhealthy만 검증, 구체 사유는 인증된 /health/detailed에서 검증), `packages/shared/schema/openapi.json`(재export). 앱 로직 변경 없음 — 테스트만 M9 계약에 정합.
- **검증**: 풀 백엔드 **607 passed**(regression-0 복구). M9 explosion-radius가 예고한 H1 공유 불변식 충돌이 정확히 실현된 것.

### L18 — release 서명 fail-closed (LOW)
- **변경**: `android/app/build.gradle.kts`(buildTypes.release: key.properties 부재 시 `signingConfigs.getByName("debug")` 폴백 제거 → release 태스크(`gradle.startParameter.taskNames`에 "Release")면 `GradleException` throw, 아니면 null[debug 빌드 무관]), TDD 신규 `test/android_signing_test.dart`(2 정적 가드).
- **근거**: androiddebugkey로 서명된 '릴리스' 산출물이 Play 거부/내부 debug 유통되는 fail-open 제거. EnvConfig fail-closed 철학과 통일.
- **검증**: 2 RED→GREEN(flutter test). 실빌드 검증(key.properties 없이 --release가 실패, 있으면 release 서명)은 Android 빌드 환경 필요 — 정적 가드로 폴백문자열 부재+GradleException 존재 잠금.

### 네이티브 iOS 설정 묶음 — H27·H28·M33·L19 (Info.plist 공유, 한 커밋)
- **H27(카카오 공유 iOS 불능, HIGH)**: `ios/Runner/Info.plist`에 LSApplicationQueriesSchemes(kakaolink·kakaotalk 등 6종) + CFBundleURLTypes(스킴 `kakao$(KAKAO_NATIVE_APP_KEY)`) 추가. `ios/Flutter/Debug.xcconfig`·`Release.xcconfig`에 KAKAO_NATIVE_APP_KEY 빌드변수 선언(G28: 하드코딩 금지, CI/시크릿 주입). iOS는 미선언 스킴에 canOpenURL이 항상 false → 미선언 시 공유 100% 불능이던 근본원인 해소.
- **H28(취침 오디오 자동잠금 즉사, HIGH)**: **G27=방식(a) 백그라운드 오디오**. Info.plist에 UIBackgroundModes(audio), `pubspec.yaml`에 audio_session ^0.1.21 직접 의존(0.1.25 오프라인 해석), `lib/screens/viewer_screen.dart`에 `_configureAudioSession`(AVAudioSession playback 카테고리, initState 1회, try-catch 폴백). 수동 잠금에도 오디오 생존. **주의(운영 노트)**: Apple 백그라운드 오디오 심사 리스크 + Dart Timer는 백그라운드 정지(자동 넘김은 오디오 완료 이벤트 기반이라 생존).
- **M33(네이티브 표시명·권한 en/ja, MEDIUM)**: **G29=en/ja 앱명 영문 'AI Story Book' 통일, 권한 문구만 번역**. Android `res/values{,-en,-ja}/strings.xml` app_name 3종 + AndroidManifest label `@string/app_name`. iOS `{ko,en,ja}.lproj/InfoPlist.strings`(CFBundleDisplayName + 권한 4종 번역), pbxproj knownRegions에 ko·ja 추가.
- **L19(placeholder 도메인, LOW)**: **G28=정본 aistorybook.com**. Info.plist 미사용 example.com URL 키 2종 삭제, `kakao_share_service.dart` _webBaseUrl 기본값 aistorybook.app→aistorybook.com(설정화면과 통일).
- **TDD**: `test/native_config_test.dart`(11 정적 가드 — plist/xcconfig/pbxproj/manifest/strings/Dart상수). 11 RED→GREEN. 풀 모바일 235 pass, flutter analyze 무이슈.
- **⚠ 검증 한계(실기기/Xcode 필요, 이 가드로 대체 불가)**: (1) iOS InfoPlist.strings **variant group 리소스 등록**(pbxproj PBXBuildFile/변형그룹)은 Xcode 무결성 민감 편집이라 손편집 회피 — knownRegions+파일만 반영, **Xcode에서 InfoPlist.strings를 Localize로 타깃에 추가**해야 실기기 지역화 발효. (2) 카카오 공유 실기기 스모크(canOpenURL true), (3) 자동잠금 후 오디오 지속 스모크, (4) KAKAO_NATIVE_APP_KEY의 CI 실주입. 모두 디바이스/Xcode 필요 — FIXLOG에 잔여로 명시.
