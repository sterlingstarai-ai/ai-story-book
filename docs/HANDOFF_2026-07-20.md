# 구현 핸드오프 — AI Story Book 출시 전 결함 수정 (2026-07-20)

**수임: 구현 전담 개발자 / 발주: CTO(설계·감사)**

## 읽는 순서
1. **`docs/DEV_SPEC_2026-07-20.md`** (v1.2) — 개발 명세 정본. **§0 브리프를 먼저 통독**한 뒤 웨이브 순서로 착수.
2. `docs/CODE_REVIEW_2026-07-20.md` — 근거 감사(확정 86건). 티켓의 "왜"가 궁금할 때 참조.
3. `docs/DECISIONS_NEEDED_2026-07-20.md` — 규범 결정 근거. 결정값은 이미 DEV_SPEC §2에 주입됨.

## 지금 상태
- **감사**: 전체 코드베이스 적대 감사로 86건 확정(치명3·높음28·보통34·낮음21) + 편입 1건(N1).
- **명세**: 87티켓 풀스펙(증상·근본원인·수정단계·TDD테스트·DoD·의존·폭발반경). 핸드오프 전 6렌즈 적대 자기검토로 v1.1 정정(블로커5·메이저13·마이너5) → 25티켓에 **`⚠ 핸드오프 검토 정정`** 블록(원 fix보다 **우선**).
- **결정**: 규범 게이트 **34건 전건 확정**(미결 없음). 브랜드(G28·G29): 정본 도메인 **aistorybook.com**, 카카오 딥링크 수신 **1차 제외**, 앱 표시명 **영문 'AI Story Book' 통일**.
- **코드**: **아직 한 줄도 수정 안 함.** 문서만 존재(전부 미커밋).

## 착수 규칙 (§0 요약)
- **TDD**: 버그 재현 테스트 먼저(실패 확인) → 수정. 티켓별 테스트 명세 있음.
- **베이스라인**: 백엔드 pytest 434 pass / **1 known-fail**(openapi 버전 — 로컬 `.env` APP_VERSION 잔재, M8을 W1로 승격해 그린화) · 모바일 194 pass. 게이트 판정은 "전체 그린"이 아니라 **"베이스라인 대비 회귀 0"**.
- **검증 루프**: `venv/bin/python -m pytest tests/` · `venv/bin/ruff check src/` · `flutter test`(3.38.7) · 신규 문자열 시 `flutter gen-l10n`(ko/en/ja 동시).
- **DB**: `down_revision` 하드코딩 금지, §3.1 리비전 총순서 준수, `alembic heads` 단일 확인. money 마이그레이션은 실DB(PG) 리허설(§3.1).
- **커밋은 오너가**: 티켓/커밋 그룹 단위 staged 준비 → 오너 커밋 → 다음 착수. 웨이브 게이트 그린이 롤백 체크포인트.
- **범위 밖**: 기각 7건·미구현 제품기능·광범위 리팩터(§0.2).

## 웨이브 순서
W1 치명 3 → W2 IAP/구독 돈코어 → W3 POD·오케스트레이터·모바일 이중차감 → W4 글로벌 i18n+법정동의/삭제 → W5 데이터유실·삭제권·동시성 → W6 인프라·CI → W7 품질. **각 웨이브 종료 시 전체 게이트로 회귀 0 확인 후 다음.**

## 주의: 창업자 결정으로 스코프가 커진 항목
- **타임존(G10)**: CTO 권장(KST 고정)이 아니라 **사용자별 타임존 도입**으로 확정. W4의 H2가 user_settings IANA timezone 컬럼 + local_* 헬퍼 tz 인지화(L 규모). DST는 zoneinfo로, 고정 offset 덧셈 금지.

## 완료 판정
DEV_SPEC §5(최종 통합 검증 원샷 + 인수 체크리스트) 충족 시 done. 완료는 웨이브별로 오너에게 보고.
