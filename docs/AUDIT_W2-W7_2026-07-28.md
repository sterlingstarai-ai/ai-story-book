# CTO 감사 보고서 — DEV_SPEC v1.3 W2→W7 구현 (2026-07-28)

> **대상**: 브랜치 `feat/global-multilang-product-rollout-20260622` HEAD `a5d0757` (`fab7bd8..HEAD` 55커밋, 84티켓)
> **방법**: 보고서 불신 원칙 — 게이트 직접 재실행 + 마이그레이션 실DB 재현 + 커밋 diff 7렌즈 적대 감사(→트리아지→3렌즈 반증→머더보드). 확정 20 / 기각 9.
> **판정**: ❌ **클린 PASS 아님.** 치명 1건(출시 차단) + 높음 2건은 **푸시/출시 전 수정 필수**. 인수 게이트·마이그레이션 무결성은 통과.

---
## 0. 독립 검증 (CTO 직접 재현 — PASS)
- **인수 게이트 재실행**: 백엔드 `pytest` **619 pass / 0 fail**, `ruff` clean, alembic 단일 head `f6a7b8c9d0e1`; 모바일 `flutter test` **242 pass**, `analyze` 무이슈 — 보고서 수치 일치.
- **마이그레이션 실 Postgres 체인 재현**: W2~W7 신규 7개를 base부터 populated 데이터에 적용 → dedup 무결성(credit_transactions refund/purchase·daily_stories) + 부분유니크 차단 + NULL 제외 정확, 단일 head 도달.
- **`daily_stories` date-단독 unique 검증**: DailyStory가 전역 테이블(user_key 없음)임을 모델·코드로 확인 → 올바름(재앙적 오류 아님).
- **핵심 교훈**: 게이트가 전부 green이어도 **치명 결함이 통과**했다(아래 C1/MA1). green ≠ correct — false-green 테스트가 원인.

---
## 1. 확정 결함 20건 (반송 티켓 — 각 fix 포함)


### 🔴 CRITICAL — 출시 차단

**1. [C1(MA1)] MA1 미구현(false-green): Apple 실검증이 expires_date_ms를 추출하지 않아 만료 영수증으로 무한 구독·크레딧 리필 가능**
- 위치: `apps/api/src/services/iap_verifier.py:135`
- 증상/근거: 4d0933b은 IAPVerificationResult에 expires_date_ms 필드만 추가했고(3줄 diff 전부), 실제 Apple 검증 경로(_verify_apple 135-149행)는 Apple 응답의 expires_date_ms를 추출하지 않는다. 라우터의 _subscription_expired(iap.py:71-77)는 getattr→None→False로 항상 통과. MA1 테스트(test_expired_receipt_restore_creates_no_active_sub)는 monkeypatch로 verify_purchase 전체를 fake로 바꿔 expires_date_ms=1을 주입하므로 green — 실 코드 경로는 한 번도 검증되지 않음. 실증: strict 모드에서 _post_apple_receipt만 mock(만료 latest_receipt_info 응답)한 프로브 실행 결과 verified=True, expires_date_ms=None 확인. Google은 iap_verifier.py:405-409에서 만료를 실검사하므로 Apple만 열려 있음.
- 트리거: 운영(strict 모드, 출시 필수 설정)에서 만료된 Apple 자동갱신 구독 영수증을 verify/restore — Apple verifyReceipt는 만료 구독도 status=0 + latest_receipt_info로 반환하므로 검증 통과. 영수증은 사용자의 기기/계정에 그대로 남아 있어 누구나 재제출 가능. 만료 영수증엔 웹훅이 오지 않아 H4/H5 조정 경로로도 회수 불가(스펙 MA1 문구 그대로).
- 영향: 1개월 구독 1회 결제 후 만료 → 만료 영수증 재제출로 active 구독 재생성(첫 verify면 +30크레딧 즉시 지급) → periodic_credits.grant_due_refills가 30일마다 무기한 자동 갱신·30크레딧 리필. 실결제 없는 영구 프리미엄 = C1 제목의 '무한 수익화'가 미봉인 상태. FIXLOG는 'MA1 완료·FALSE-GREEN-RISK 없음'으로 보고해 보고서-코드 불일치.
- 수정: _verify_apple에서 matched.get('expires_date_ms')를 _parse_int로 추출해 IAPVerificationResult(expires_date_ms=...)에 실어 반환(구독 상품일 때). Google도 expiryTimeMillis를 동일 필드에 실어 일관화. 테스트는 verify_purchase 전체 mock이 아니라 _post_apple_receipt 층 mock으로 실경로를 통과시키는 회귀 테스트로 교체.


### 🟠 HIGH — 출시 전 수정

**2. [H1(G9)] G9 '명시 비활성'의 절반만 구현 — 오디오 기능 플래그가 readiness에만 배선되어 출시 구성에서 오디오·발음 탭마다 500**
- 위치: `apps/api/src/main.py:479`
- 증상/근거: audio_feature_enabled(config.py:72, 기본 false)는 main.py:479 readiness 게이트에만 쓰인다. 오디오/발음 엔드포인트에는 플래그 검사도 NOT_SUPPORTED 응답도 없고, /v1/config/capabilities(routers/config.py:13)에 inpaint_supported만 있어 모바일이 오디오 가용성을 알 수 없다(모바일은 이미 capabilitiesProvider로 인페인트를 게이팅 중 — 오디오만 누락). G9 출시 구성(플래그 off + TTS_PROVIDER 미설정)에서 lazy provider 해석이 ValueError raise → get_page_audio는 InternalServerError 500, pronunciation evaluate는 무처리 500, 배치 오디오 잡은 전건 failed.
- 트리거: G9 결정대로 오디오 비활성 GA 후, 아무 사용자가 뷰어 낭독 버튼/발음 연습을 탭.
- 영향: GA 출시 상태에서 비독자 아동의 핵심 소비 수단(낭독) UI가 전부 살아있는 채 매 탭 에러 스낵바. fail-open(무음 서빙)은 제거됐지만 '명시적 기능 비활성'(기능 숨김/NOT_SUPPORTED)이 아니라 '전면 500'이 출시 동작. CS·스토어 리뷰 리스크.
- 수정: capabilities에 audio_supported(=audio_feature_enabled && provider live) 추가 + 모바일 뷰어/발음 UI 게이팅(인페인트 패턴 재사용), 또는 오디오 엔드포인트에서 플래그 off 시 4xx NOT_SUPPORTED 명시 응답. 아니면 GA 구성을 오디오 활성(라이브 TTS/STT)으로 확정.

**3. [N1] 단건 책 삭제·동의 철회가 파이프라인 이미지(images/{provider}/…)를 파기하지 않음 — 행 삭제로 역산 키까지 소실되어 아동 likeness 일러스트가 영구 잔존·영구 추적불가**
- 위치: `apps/api/src/routers/consent.py:176`
- 증상/근거: N1의 역산 삭제(collect_book_image_keys→delete_keys)는 계정 삭제(users.delete_my_data)에만 배선됐다. 동의 철회(consent.py:142-186)와 단건 책 삭제(library.py:211-218)는 purge_book_children+delete(Book)으로 Page/Book 행(=image_url)을 먼저 지운 뒤 delete_book_files(books/{id}/ prefix)만 호출한다. 파이프라인 표지·페이지 이미지는 images/{provider}/{uuid}에 있으므로 이 prefix에 없고, 유일한 추적 수단인 image_url 행이 같은 트랜잭션에서 삭제되어 이후 계정 삭제의 역산으로도 다시는 찾을 수 없다. N1 스펙 DoD·테스트 명세는 '책 단건 삭제 시에도' 파기를 명시(2586절)했으나 FIXLOG는 N1 완료로 보고.
- 트리거: 사진 기반 캐릭터로 책 생성 → (a) POST /v1/consent/revoke(사진 동의 철회) 또는 (b) DELETE /v1/library/{book_id} → 표지·페이지 이미지가 공개 URL(s3_public_url/images/…)에 그대로 남음. 철회는 PIPA/COPPA 파기 의무의 가장 강한 트리거인데 바로 그 경로가 뚫려 있음.
- 영향: 아동 얼굴 likeness가 렌더된 일러스트가 동의 철회 후에도 공개 스토리지에 영구 잔존(PIPA/COPPA/GDPR 파기 의무 위반). 행 삭제로 키 역산이 불가능해져 사후 배치 정리도 못 함(영구 고아).
- 수정: consent.revoke와 library.delete_book에서 행 삭제 전 collect_book_image_keys(db, book_ids)로 키를 수집하고 delete_keys로 파기(users.delete_my_data와 동일 순서: 수집→행 삭제→commit→파기). 실패키는 H8 계약대로 로그/partial 표면화. 두 경로 각각 회귀 테스트 추가.


### 🟡 MEDIUM

**4. [M12/H24] 재생성·리텔·인페인트 모더레이션이 ja/zh/es에서 여전히 no-op (키워드망만 사용, H24 LLM 폴백 미적용)**
- 위치: `apps/api/src/services/orchestrator.py:1271`
- 증상/근거: M12가 배선한 안전 게이트가 전부 _moderate_text(ko/en 키워드망)뿐이다: regenerate feedback 입력(orchestrator.py:1227), regenerate 출력(1271), inpaint region_prompt(1362), retell 출력(books.py:1161). H24가 메인 파이프라인 moderate_output에는 ja/zh/es LLM 폴백(call_output_moderation)을 배선했지만 이 4개 경로에는 적용하지 않아, ja/zh/es 텍스트는 입력·출력 모두 무조건 통과한다. 스펙 M12 fix_steps가 _moderate_text만 지시한 것은 사실이나, 같은 웨이브의 H24가 확립한 '키워드망 밖 언어=fail-open' 불변식과 정면 모순(파리티 목표 미달).
- 트리거: 인증된 사용자가 자기 ja/zh/es 책에 API로 POST regenerate(mode=text, feedback='オオカミがウサギを殺して血まみれにする') 호출 — _moderate_text는 ja 키워드가 없어 True, LLM이 폭력 본문 생성, 출력 검사도 True → page.text에 저장. 앱에서는 인페인트 화면의 region_prompt 자유 텍스트로도 도달(이미지 쪽은 provider 필터가 부분 완충).
- 영향: 출시 5개 언어 중 3종에서 아동 안전망을 우회해 폭력·성인 표현을 책 본문에 저장 가능. book_shares 공개 공유 링크로 타 아동에게 노출될 수 있음(아동 앱 심사·규제 리스크). H24가 '텍스트 안전 공백 완전 해소'로 보고했으나 재생성 계열 경로는 미해소.
- 수정: _moderate_text 통과 후 book.language가 _KEYWORD_COVERED_LANGUAGES(ko/en) 밖이면 call_output_moderation(text, language) 폴백을 4개 경로(regenerate 입력/출력, retell 출력, inpaint region_prompt)에 재사용. H24와 동일 패턴이라 소규모.

**5. [M26] deploy.sh: up -d 직후 무대기 health_check → 정상 릴리스도 스퓨리어스 자동 롤백**
- 위치: `scripts/deploy.sh:291`
- 증상/근거: deploy case가 start_services(compose up -d, 290행) 직후 즉시 health_check(291행)를 호출하는데, health_check(198-212행)는 curl -fsS 1회씩(재시도·대기 없음)이다. compose up -d는 컨테이너 '기동 시작'에서 리턴할 뿐 앱 리슨을 기다리지 않고(--wait 미사용, compose healthcheck start_period 20s 무시), api 2 replica가 동시에 재생성되는 동안 nginx(api 재생성으로 함께 재생성됨)는 502를 반환한다. FastAPI 앱 임포트(19개 라우터+SQLAlchemy)는 수 초가 걸리므로 첫 curl은 사실상 항상 실패한다. 구 흐름은 up→migrate→health 순서라 migrate 실행 시간이 우연히 부팅 대기 역할을 했는데, M26이 migrate를 앞으로 옮기며 이 암묵 대기가 사라졌고, 동시에 health 실패에 '자동 롤백'을 새로 배선해 실패의 결과가 치명적으로 커졌다. rollback() 내부 재-health_check(241행)도 동일하게 무대기라 롤백조차 '실패' 판정된다.
- 트리거: CI main 머지 → deploy 잡 → deploy.sh deploy 실행되는 모든 릴리스(api 이미지가 바뀌는 모든 배포). 특수 조건 불필요.
- 영향: 사실상 모든 배포가 health 실패로 판정 → 신 이미지가 정상인데도 이전 이미지로 자동 롤백(그마저 재-health 실패로 'manual intervention required') → CI deploy 영구 red, GA 시점에 CI 경유 릴리스 불능. 부수적으로 마이그레이션은 이미 적용된 상태에서 구 이미지로 되돌아가는 churn 반복.
- 수정: start_services 후 대기 삽입: (a) `compose up -d --wait`(compose healthcheck start_period 20s 활용) 또는 (b) health_check에 재시도 루프(예: 30회×2s, /health/live 최초 통과까지) 추가. rollback() 경로에도 동일 적용. test_deploy_script.py에 '대기/재시도 존재' 가드 추가.

**6. [H4] Google 접미사 orderId의 orphan 웹훅이 verify 시 재적용되지 않음 — 선도착 환불 결정 영구 유실**
- 위치: `apps/api/src/routers/iap.py:538`
- 증상/근거: 스펙 H4 step2는 'Google은 _strip_google_order_suffix로 정규화한 base도 비교'를 요구하고, 직접 매칭 경로(_apply_webhook_status:514-526)는 이를 구현했으나, (a) orphan 적재 시 request.transaction_id를 정규화 없이 원본 그대로 저장하고(538행) (b) _reapply_orphan_events(488-491행)는 event.transaction_id를 receipt.transaction_id/store_transaction_id와 원문 등호 비교만 한다. 접미사(..0/..1)가 붙은 Google orderId로 선도착한 환불/취소 웹훅은 orphan으로 적재만 되고 이후 verify에서 영원히 applied=False로 잔존한다.
- 트리거: Google 구독: 사용자가 결제했으나 클라이언트 verify가 지연/실패(앱 크래시·오프라인)한 상태에서 갱신 주문(orderId 'GPA.x..N')에 대한 환불·취소 통지가 먼저 도착 → orphan 적재. 이후 사용자가 verify(스토어가 base orderId 반환) → 재적용 쿼리 미스.
- 영향: 환불 결정 영구 유실: 환불된 구독이 active로 남아 periodic_credits가 매월 리필 — H4가 막으려던 buy→refund 무한 무료 크레딧이 Google 접미사 케이스에서 그대로 재현. provider는 200을 받았으므로 재전송도 없음.
- 수정: orphan 적재 시 platform=='google'이면 _strip_google_order_suffix로 정규화한 값을 함께(또는 정규화 값으로) 저장하고, _reapply_orphan_events의 or_ 매칭에 정규화 비교를 추가. 테스트: 접미사 orderId 선도착 → verify 재적용 케이스를 test_iap_hardening에 추가.

**7. [H5] 만료 영수증 restore 후 남는 타인 구독 참조(FK dangling) — 이전 소유자 계정 삭제가 FK 위반으로 500**
- 위치: `apps/api/src/routers/iap.py:243`
- 증상/근거: restore 경로에서 만료 영수증(MA1)은 구독을 재생성하지 않는데(243행 분기), 이때 receipt.user_key는 새 사용자로 이전되면서 receipt.subscription_id는 이전 소유자의 구독 id를 가리킨 채 남는다. iap_receipts.subscription_id→subscriptions.id FK(db.py:435, 마이그레이션 b2c3d4e5f6a7:49 — ondelete 없음)가 있으므로, 이전 소유자가 DELETE /v1/users/me를 호출하면 users.py:84의 delete(Subscription)이 FK 위반으로 실패한다. 삭제받는 사용자의 IAPReceipt는 78행에서 지워지지만 문제 영수증은 이미 새 user_key 소유라 남는다.
- 트리거: 구독 만료된 사용자가 기기 변경/재설치로 새 user_key에서 restore(만료 영수증) → 이후 옛 user_key로 데이터 삭제 요청(옛 기기에서 삭제 후 이탈 등). 운영 PG는 FK가 실제 생성되므로 동일 실패.
- 영향: 법적 삭제권(GDPR/PIPA) 경로가 복구 불가 500 — 수동 DB 수정 전까지 해당 사용자는 계정/아동 데이터 삭제 불가. H8(erasure 완전성)의 폭발 반경.
- 수정: (1) FK에 ondelete='SET NULL' 부여(모델+PG 마이그레이션 ALTER), 또는 (2) restore의 만료 분기에서 existing.subscription_id=None으로 명시 해제 + erasure에서 타인 소유 영수증의 자기 구독 참조를 사전 nullify. 테스트: 위 시나리오를 test_data_deletion 계열에 추가.

**8. [M12] 모바일 텍스트/전체 재생성 메뉴가 항상 422 실패 — 서버 feedback 필수화에 모바일 배선 부재**
- 위치: `apps/mobile/lib/services/api_client.dart:152`
- 증상/근거: M12가 RegeneratePageRequest에 mode=text/both→feedback 필수 validator(dto.py:384-391)를 추가했지만, 모바일 regeneratePage는 {'mode': target}만 전송(api_client.dart:152 — feedback 파라미터 자체가 없음)하고 뷰어는 feedback 입력 UI 없이 'text'/'both' 메뉴를 노출(viewer_screen.dart:1506/1520). 스펙 폭발반경이 '모바일 재생성 호출부와 계약 접점'을 명시 경고했으나 모바일 측 대응 없음.
- 트리거: 사용자가 뷰어에서 페이지 재생성 → '텍스트만' 또는 '모두' 탭.
- 영향: 해당 메뉴가 100% 실패(viewerRegenerateFailed 스낵바). 이전에는 silent no-op을 '완료'로 위장하던 사전 결함이라 순수 회귀는 아니지만, GA 앱이 항상 실패하는 메뉴를 노출하는 상태로 출시됨. 크레딧 차감은 없음(422 선차단).
- 수정: 뷰어에 feedback 입력 다이얼로그 추가 + api_client.regeneratePage에 feedback 전달, 단기 미배선 시 text/both 메뉴 숨김.

**9. [H17/H18(MA7·G19)] retell/from-photo/from-drawing 서버측 멱등 미구현 — W3→H17(W4) 이관 후 '별도 서브커밋 예정'이 GA까지 미착륙(2회 이월)**
- 위치: `apps/api/src/routers/books.py:1117`
- 증상/근거: G19 결정과 정정 MA7이 요구한 서버측 멱등 수용 + 클라 시도 멱등키가 미구현: retell_book(books.py:1117-1123)에 get_idempotency_key 의존성이 없고 characters from-photo/from-drawing도 동일, openapi에도 미문서(MA7의 '미수용 헤더 문서화 금지' 계약 원칙은 준수). FIXLOG W4 항목(:305)이 '미완(H17 잔여)·별도 서브커밋으로 처리 예정'으로 자인했으나 이후 55커밋 어디에도 없이 W7 완료·GA 태그까지 미착지. receiveTimeout 180s 상향만 이행 — 스펙상 from-drawing 요청 내 처리는 최대 ~330s라 180s로도 부족. retell은 크레딧을 차감하지 않음을 확인(스펙의 '돈 버그' 주장은 코드 기준 과장 — 크레딧 이중차감은 아님).
- 트리거: 느린 이미지 생성 시 from-drawing/from-photo가 180s에서 클라 타임아웃 → 서버는 완주해 캐릭터 생성(orphan) → 사용자 재시도 → 중복 캐릭터. retell도 동일 패턴(요청 타임아웃 후 재시도 → 중복 리텔 책).
- 영향: 중복 캐릭터/리텔 책 생성, vision·LLM 비용 낭비, 서재 오염. H18이 막은 book 생성 이중차감과 달리 이 3개 엔드포인트는 방어 없음(크레딧 이중차감은 아님).
- 수정: create_series_next의 H18 패턴(get_idempotency_key 조회→기존 리소스 반환) 미러 + 클라 시도키 전송 + 계약 문서화(잔여 티켓으로 명시 트래킹). 또는 G19를 '미구현 수용'으로 공식 재결정.

**10. [N1] 페이지 재생성·인페인트가 이전 이미지 키를 삭제하지 않고 image_url을 덮어써 구버전 이미지가 영구 고아화 — 계정 삭제 역산도 '현재 URL'만 커버**
- 위치: `apps/api/src/services/orchestrator.py:1313`
- 증상/근거: regenerate_page(1313-1315)와 inpaint_page(1382-1384)는 새 이미지 생성 후 page.image_url을 교체만 하고 이전 키(images/{provider}/{uuid})를 삭제하지 않는다. collect_book_image_keys는 현재 image_url만 역산하므로, 교체 전 버전은 어떤 삭제 경로(계정/책/철회)로도 파기되지 않는다. 실패한 잡에서 생성된 이미지(행에 영속되기 전 단계 실패)도 동일하게 추적 불가 고아.
- 트리거: 사용자가 페이지 재생성 또는 인페인트 실행(핵심 출시 기능) 후 계정 삭제 — 교체 전 일러스트(아동 사진 파생 가능)가 스토리지에 잔존.
- 영향: N1이 닫으려던 것과 동일한 클래스의 erasure 잔여 갭(구버전 한정) + 스토리지 비용 누수. 아동 사진 참조(gemini face reference) 구성에서는 PII 잔존.
- 수정: 이미지 교체 성공 커밋 후 이전 image_url의 역산 키를 delete_keys로 즉시 파기(실패는 warning). 또는 교체 이력 테이블/키 목록을 남겨 삭제 경로에서 일괄 파기.

**11. [M23] M23 재전달 멱등화가 실질 미달성(false-green) — mid-flight 재전달은 여전히 확정 UNKNOWN 실패, 태스크 레벨 IntegrityError 흡수는 사실상 사후 도달**
- 위치: `apps/api/src/services/tasks.py:159`
- 증상/근거: M23이 의존한 '산출물 멱등화(H10/orchestrator 쪽 upsert)'는 착지하지 않았다: save_story_draft/save_image_prompts(orchestrator.py:1164-1183)는 여전히 plain INSERT다. 워커가 draft 저장 후 죽어 재전달되면 status='running'이라 가드를 통과해 재실행→INSERT 충돌인데, 이 IntegrityError는 start_book_generation 자신의 전역 except(orchestrator.py:482-484)가 먼저 잡아 mark_job_failed(UNKNOWN)+환불 후 re-raise하므로, tasks.py:159의 '흡수'는 잡이 이미 failed 처리된 뒤에야 도달한다(로그 'absorbing idempotent redelivery collision'은 오해 유발). 즉 티켓 헤드라인 시나리오(복구 가능한 재전달)가 여전히 영구 실패다. test_tasks.py는 start_book_generation을 mock해 이 실경로를 검증하지 못함(버그가 있어도 green).
- 트리거: 배포(compose down은 celery 워커를 SIGTERM/KILL) 또는 워커 크래시가 생성 파이프라인 중간(draft 저장 후)에 발생 → acks_late 재전달 → 재실행 충돌. 모든 스키마 배포마다 in-flight 잡이 이 경로를 탄다.
- 영향: 돈은 mark_job_failed의 멱등 환불로 방어되나, 결제된 생성이 '복구' 대신 UNKNOWN 실패로 확정(스펙 M23 목표 미달). 사용자 재시도 필요·실패율 지표 오염·오해성 로그로 운영 진단 혼란.
- 수정: 스펙대로 save_story_draft/save_image_prompts를 job_id 기준 upsert(있으면 갱신/스킵)로, package_book을 기존 book 행 존재 시 재사용으로 멱등화. 그 후 tasks.py의 IntegrityError 특례는 제거 또는 진짜 dup에 한정. 재전달 시나리오를 mock 없는 통합 테스트로 고정.

**12. [H7] erasure 회귀 테스트(test_data_deletion_fk.py)가 실 네트워크 S3 호출로 비결정적 — GDPR 게이트가 flaky(false-green/false-red 위험)**
- 위치: `apps/api/tests/test_data_deletion_fk.py:103`
- 증상/근거: 이 파일을 3회 실행에 3회 모두 서로 다른 테스트가 실패했다(1회차: shares 계정삭제 FAILED+voice/pipeline ERROR, 2회차: shares FAILED+voice ERROR, 3회차: 단건삭제 FAILED+pipeline teardown ERROR — 'no such table: books/jobs/quiz_answers'). 원인 구성요소는 코드로 확인됨: (a) 여러 삭제 테스트가 storage를 monkeypatch하지 않아 실제 boto3가 localhost:9000에 네트워크 호출(런당 60초+ 지연, 실행 로그의 'Could not connect' warning) — 본 finding의 고유 요소, (b) 전 테스트가 파일 기반 ./test.db 하나를 create_all/drop_all로 공유해 지연된 비동기 작업이 테스트 경계를 넘으면 스키마가 사라진 상태에서 실행됨 — 이 (b) 요인은 TEST-INFRA 통합 finding과 동일 뿌리.
- 트리거: 로컬/CI 러너의 네트워크 지연·타이밍에 따라 무작위 실패 또는 무작위 통과. (주의: 본 감사 샌드박스의 네트워크 처리 특성이 지연을 증폭했을 가능성은 있으나, 실 boto3 호출·공유 파일 DB 구조 자체는 코드 사실.)
- 영향: H7/H8/N1(GDPR/PIPA 핵심 경로)의 회귀 게이트가 신뢰 불가 — flaky 게이트는 실제 erasure 회귀를 '재실행하면 green'으로 통과시킬 수 있음. FIXLOG의 '543 pass' 증거력이 이 파일에 한해 약함.
- 수정: storage 경계를 conftest 수준에서 기본 mock(autouse fixture)하고 실 S3는 별도 통합 마커로 분리. 테스트 DB를 파일 공유 대신 per-test in-memory(또는 tmp_path 파일)로 격리해 스키마 경합 제거.

**13. [M33] iOS InfoPlist.strings가 Xcode 프로젝트에 미등록 — en/ja 지역화가 코드상 발효 불능**
- 위치: `apps/mobile/ios/Runner.xcodeproj/project.pbxproj:237`
- 증상/근거: 커밋 44ad484가 {ko,en,ja}.lproj/InfoPlist.strings 3파일과 knownRegions(ko·ja)만 추가했고, pbxproj에 InfoPlist.strings의 PBXFileReference/PBXVariantGroup/Resources 빌드페이즈 등록이 전무하다(grep 'InfoPlist|.strings' 0건 — 기존 storyboard variant group만 존재). 프로젝트에 미참조된 파일은 어떤 빌드에도 번들되지 않으므로, iOS에서 M33의 표시명·권한 문구 지역화는 현재 코드 상태로는 어떤 빌드에서도 동작하지 않는다. 구현자는 FIXLOG에 'Xcode에서 Localize 추가 필요'로 자가 공개했으나, variant group 추가는 순수 텍스트 편집이고 스펙 M33이 'pbxproj knownRegions·리소스 참조 편집'을 명시 스코프로 포함하므로 환경 제약이 아니라 코드 미완이다. 가드 native_config_test는 파일 존재+knownRegions만 검사해 이 미배선 상태에서 green(false-green).
- 트리거: 모든 iOS 빌드. en/ja 기기 사용자가 앱 설치·권한 프롬프트(카메라/사진/마이크/음성인식)를 볼 때.
- 영향: 글로벌 GA인데 en/ja iOS 사용자에게 앱 이름 'AI 동화책'·권한 목적 문구가 전부 한국어로 노출(G29 결정 위반 상태 지속). 아동 대상 앱의 권한 목적 문구 비지역화는 App Store 심사 리스크. Android 절반만 실동작.
- 수정: pbxproj에 InfoPlist.strings PBXVariantGroup(ko/en/ja 자식 PBXFileReference) + PBXBuildFile + Runner Resources 페이즈 등록을 텍스트로 추가(기존 Main.storyboard variant group 패턴 복제). native_config_test에 'pbxproj가 InfoPlist.strings를 참조'하는 가드 추가. 이후 Xcode 프로젝트 무결성 1회 검증(xcodebuild -list).


### ⚪ LOW

**14. [C1] 만료 영수증 restore가 이전 소유자의 정당한 same-plan active 구독까지 만료시킴 (expire가 MA1 만료검사보다 먼저 실행)**
- 위치: `apps/api/src/routers/iap.py:237`
- 증상/근거: restore 분기에서 expire_active_subscription_for_plan(이전 소유자, plan) 호출(237-240행)이 _subscription_expired 검사(243행)보다 앞에 있고 무조건 실행된다. 영수증이 스토어에서 이미 만료되어 아무 entitlement도 이전하지 않는 경우에도 이전 소유자의 해당 plan active 구독(이 영수증과 무관한 더 최신 결제의 구독일 수 있음)을 즉시 expired로 만든다. MA1 테스트도 old-owner 구독 만료 여부는 assert하지 않아 미검출.
- 트리거: 사용자 O의 옛 만료 구독 영수증(IAPReceipt 소유 O)이 다른 user_key로 restore됨 — 재설치 시 앱이 과거 트랜잭션을 재제출하는 정상 플로우, 또는 유출된 옛 영수증으로 타인 구독을 죽이는 공격. O는 그 사이 같은 plan을 신규 결제해 active 구독 보유.
- 영향: 정당하게 결제된 active 구독이 만료 영수증 restore 한 번에 소멸 → 유료 사용자 권한 상실·CS/환불. MA1(expires_date_ms 미추출)을 고쳐도 이 순서 결함은 그대로 남는다(만료 가드가 신규 구독 생성만 막고 expire는 못 막음).
- 수정: restore 분기에서 _subscription_expired(verification)이면 이전 소유자 expire와 신규 구독 생성을 모두 건너뛰고 영수증 소유 이전만 수행(또는 만료 영수증은 restored 처리 자체를 거부). 테스트에 '만료 영수증 restore 시 이전 소유자 active 구독 불변' assert 추가.

**15. [H6/L6] GET /v1/pod/orders/{id}가 Printful 타임아웃 시 500 — PodSubmitUnknown이 상태조회 경로에서 미포획**
- 위치: `apps/api/src/services/pod_provider.py:288`
- 증상/근거: _printful_request는 GET/POST 구분 없이 httpx.TimeoutException을 PodSubmitUnknown으로 변환한다. create_order와 reconcile_by_external_id는 이를 잡지만, sync_order_status(146-160행)는 except ValidationError만 잡고, 라우터 get_pod_order(pod.py:274)도 except ValidationError만 잡는다 → 상태 동기화 중 타임아웃이면 PodSubmitUnknown이 그대로 전파되어 미처리 500.
- 트리거: hybrid/strict 운영에서 provider_order_id가 있는 주문의 GET 조회 중 Printful 응답 지연(>20s)·커넥션 타임아웃 — provider 장애의 최빈 형태. L6가 방어하려던 바로 그 시나리오.
- 영향: 주문 상태 화면이 통째로 500(모바일 에러) — L6 DoD '장애 시 로컬 스냅샷 200' 위반. 데이터 손상은 없으나 실물 주문 추적 가용성 상실.
- 수정: sync_order_status에서 except (ValidationError, PodSubmitUnknown)으로 잡아 로컬 스냅샷(PodStatusResult(status=current_status, sync_source='sync_failed'))을 반환하거나, 라우터 get_pod_order의 except를 (ValidationError, PodSubmitUnknown)으로 확장. GET 타임아웃 회귀 테스트 추가.

**16. [H6/H12] strict 모드 제출 확정실패 후 멱등 재시도가 미제출 주문(pending_submit)을 '접수 성공'으로 응답 — 재제출·대사 경로 전무**
- 위치: `apps/api/src/routers/pod.py:170`
- 증상/근거: strict 모드에서 Printful이 400/5xx로 확정 실패하면 ValidationError가 라우터로 전파되어 사용자는 400을 받지만 선기록된 pending_submit 행은 남는다. 모바일은 실패 시 멱등키를 유지하므로 재탭 시 161-171행 멱등 조회가 그 pending_submit 행을 200으로 반환하고 외부 재제출을 하지 않는다. 모바일 _submitOrder는 200이면 podOrderSubmitSuccess 스낵바를 띄운다. pending_submit/pending_provider 행을 재제출·대사하는 경로는 코드 어디에도 없다(reconcile은 submit_unknown 전용).
- 트리거: strict(또는 hybrid의 pending_provider) 운영에서 Printful 일시 장애 중 주문 → 사용자 재시도. 일시 장애가 지나가도 해당 주문은 영구 미제출.
- 영향: 사용자는 실물책 주문이 접수됐다고 믿지만 provider에 영원히 제출되지 않는다(영구 미이행 주문). 운영 알림·자동 회복 부재로 CS/분쟁 리스크.
- 수정: 멱등 조회에서 existing.status가 pending_submit(및 정책상 pending_provider)이면 기존 행을 재사용하되 외부 제출을 재시도하도록 분기(재시도 성공 시 행 갱신). 최소한 GET 경로/운영 배치에 pending_submit·pending_provider 재제출 대사를 추가하고 모바일이 두 상태를 '접수 완료'로 표시하지 않게 구분.

**17. [H2] get_reading_report/get_reading_history가 tz 미스레딩(KST 고정) — 스펙 fix step 4 명시 대상 누락, FIXLOG 미공개**
- 위치: `apps/api/src/services/streak.py:720`
- 증상/근거: 스펙 H2 fix step 4는 get_reading_history/get_reading_report를 tz 스레딩 대상으로 명시했으나, 둘 다 load_user_tz 없이 기본 KST 경계 사용(streak.py:695, 720-721, 736, 749, 796-797; 719행에 'KST 로컬 하루 경계로(한국 부모 기준)' 구주석 잔존). 스트릭·캘린더(tz-aware)와 리포트(KST) 간 날짜 귀속 불일치. FIXLOG H2 항목은 record_reading/get_streak_info/_get_profile_streak만 나열 — 이 누락은 미공개 부분이행.
- 트리거: timezone이 비KST로 설정된 사용자(현재는 모바일 미전송으로 인해 잠복이나, 모바일 배선 즉시 발화)의 성장 리포트/읽기 이력 조회.
- 영향: 같은 읽기가 캘린더에는 7/6, 주간 리포트에는 7/7로 귀속되고 to_date가 사용자에겐 내일 날짜로 표시되는 모순 — 부모 대시보드 신뢰 훼손. get_streak_info 결과(스트릭)와 리포트 일별 집계가 서로 다른 하루 정의 사용.
- 수정: 두 함수 서두에 tz = await load_user_tz(db, user_key) 추가 후 to_local_date/local_day_bounds_utc/local_today 호출 전부에 tz 전달(캘린더와 동일 패턴). 719행 KST 주석 삭제.

**18. [H6] POD 멱등 DB 계층(부분 유니크 + IntegrityError 재조회)이 false-green — 테스트가 제약 삭제 후에도 green**
- 위치: `apps/api/tests/test_phase_new_endpoints.py:1256`
- 증상/근거: test_pod_order_idempotent_returns_same_order는 순차 재요청만 검증하므로 라우터의 앱 레벨 pre-check(pod.py:160-166)만으로 통과한다. 실증: 스크래치 워크트리에서 모델의 uq_pod_orders_user_idempotency 인덱스를 제거해도 이 테스트는 그대로 PASS(1 passed). H6의 본체인 동시 더블탭 경로 — DB 부분 유니크가 IntegrityError를 내고 pod.py:195-207이 기존 행을 재조회 반환하는 분기 — 는 어떤 테스트도 커버하지 않는다. jobs/credit_transactions/subscriptions는 각각 제약 직접 위반 테스트가 있으나 pod_orders만 없다.
- 트리거: 두 요청이 동시에 pre-check를 통과하는 실제 더블탭/재시도(멀티 워커). 향후 리팩토링으로 인덱스나 absorb 분기가 회귀해도 CI가 green 유지.
- 영향: 실물 인쇄 이중 주문 = 사용자 이중 과금 + Printful 이중 실비. 회귀가 테스트에 안 잡히는 잠복 리스크.
- 수정: test_payment_integrity의 jobs 패턴 그대로: (1) 같은 (user_key, idempotency_key) PodOrder 2행 직접 insert → 2번째 IntegrityError 단언, (2) pre-check를 monkeypatch로 미스시켜 flush가 유니크 위반 → 라우터가 기존 주문 재조회 반환·외부 create 미호출 단언.

**19. [H10] Celery 예외 경로 _mark_job_failed_async에 H10 fence 미적용 — done 잡 무조건 failed 덮어쓰기+환불 가능**
- 위치: `apps/api/src/services/tasks.py:76`
- 증상/근거: orchestrator.mark_job_failed·job_monitor는 조건부 UPDATE fence가 적용됐지만, 형제 경로인 tasks.py _mark_job_failed_async는 read-then-write로 status를 무조건 'failed'로 덮어쓰고 refund_for_job을 호출한다(터미널 상태 검사 없음).
- 트리거: start_book_generation은 내부에서 예외를 전부 흡수하므로 도달 경로가 좁다 — 현실적으로는 SoftTimeLimitExceeded(soft_time_limit=600)가 mark_job_done 커밋 직후~태스크 반환 사이 밀리초 창에서 발생하거나 run_async 인프라 예외가 done 이후 발생하는 경우. 확률 낮음.
- 영향: 발생 시 done→failed 뒤집기 + 배달된 책에 환불(책+환불 이중지급, clawback 미배선 경로). 창이 좁아 low.
- 수정: orchestrator.mark_job_failed와 동일하게 UPDATE … WHERE status IN ('queued','running') 조건부 전이 + rowcount==1일 때만 환불로 교체(또는 이 헬퍼를 삭제하고 orchestrator.mark_job_failed 재사용).

**20. [H18] series/today 시도 멱등키가 대상(spec) 서명과 무스코프 — 실패 후 다른 대상 생성 시 이전 잡을 반환할 수 있음**
- 위치: `apps/mobile/lib/screens/library_screen.dart:483`
- 증상/근거: BookCreationNotifier.createBook은 _attemptSig(spec JSON)로 spec이 바뀌면 새 키를 발급하지만, library_screen의 _seriesAttemptKey와 home_screen의 todayAttemptKeyProvider는 '??='로만 유지되어 실패 후 다른 시리즈/다른 날짜의 생성 시도에도 이전 키를 재사용한다. 서버는 (user_key, key)로 기존 잡을 반환하므로 새 요청 본문은 무시된다.
- 트리거: 1차 시도가 서버에 잡을 만든 채 클라 타임아웃으로 실패 → 사용자가 포기 후 다른 책의 '다음 권'(또는 다음 날 오늘의 동화)을 생성.
- 영향: 의도한 새 생성이 조용히 no-op되고 이전 잡/책이 반환된다(이중과금은 아님 — 과소생성·혼란). 좁은 트리거라 low.
- 수정: createBook 패턴처럼 키를 대상 서명(seriesId+bookId / 날짜)과 함께 저장하고 서명이 다르면 새 키 발급.

---
## 2. 별도 판단 — 타임존(G10) 기능이 프로덕션에서 사실상 비작동
- 서버는 사용자별 타임존 배관을 대부분 완비했으나, **① 모바일이 timezone을 한 번도 전송하지 않고**(설정 payload에 키 부재, 온보딩 수집 전무 → 전 사용자 기본 'Asia/Seoul' 고정), **② `get_reading_report`/`get_reading_history`가 tz 미스레딩**(확정 LOW, spec fix step 4 누락).
- 결과: **창업자가 CTO 권장(KST 고정)을 뒤집고 선택한 G10(사용자별 타임존)의 사용자 가시 효과가 0** — 비KR 사용자 스트릭·한도·리포트가 KST 경계로 오작동. ①은 자동 패널이 '공개된 follow-on이라 은폐 결함 아님'으로 기각/확정 갈림(머더보드 기각), 그러나 사실관계는 명확하므로 CTO 판단으로 **출시 전 반드시 인지·결정할 사항**으로 승격 보고. 수정은 모바일 5~10줄.

---
## 3. 기각 9건 (오탐 — 쫓지 말 것)
- **TEST-INFRA(전 웨이브 게이트 공통 — C1·W3 '480 pass'·M34 '607 pass'·FIXLOG '619 passed 0 fail' 주장 포함)** 백엔드 pytest 게이트 자체가 비결정적(flaky): 고정 경로 공유 test.db + per-test create_all/drop_all 
- **H2** 사용자별 타임존이 프로덕션에서 사문화 — 모바일이 timezone을 한 번도 전송하지 않음
- **H24(G17)** G17 '이미지 출력 안전검사 병행 배선' 미이행 — 전 언어 이미지 안전검사 no-op 지속 (공개된 편차, CTO 재확인 대기)
- **L11** apps/api/.env.example에 POD_MODE/PRINTFUL_* 4종 미추가 — DoD 절반만 이행(infra만 반영)
- **H3(MA5)** get_page_audio 쓰기 경로에 book_language 가드 부재 — 교차 언어 요청이 기본 오디오 슬롯을 영구 오염 (MA5 절반 이
- **M5** integration-output.log가 실패 아티팩트 목록에 누락 — M5 DoD 미충족
- **M2(G30)** FIXLOG·커밋의 'HIGH는 image SARIF로 가시화' 클레임이 허위 — HIGH CVE가 어디에도 안 보임
- **L4** infra/.env의 API_IMAGE 오버라이드가 --image-tag $GITHUB_SHA를 조용히 무력화
- **MIG-PG** 돈 마이그레이션의 '데이터 있는 DB' 경로(dedup DELETE·고아 nullify·백필)가 어떤 게이트에서도 실행되지 않음 — 빈 PG 업

> 단, 기각 중 **TEST-INFRA(백엔드 pytest 공유 test.db flaky 주장)**·**H2-모바일 타임존**은 머더보드가 기각했으나 1차에서 생존한 판단 분기 항목 — §2가 후자를 승격. TEST-INFRA는 내 단일 실행에서 619 클린 통과라 블로커 아님(테스트 하드닝 후속 권고).

---
## 4. 권고
1. **출시 차단 해제 조건**: C1/MA1(치명) 수정 — Apple `expires_date_ms` 실추출 + MA1 테스트를 `_post_apple_receipt` 층 mock으로 교체(실경로 검증). H1(오디오 GA 500)·N1(단건 삭제/동의철회 이미지 파기) 수정.
2. **타임존(§2)**: 모바일 전송 배선 + reading report/history tz 스레딩, 또는 G10을 KST 고정으로 재결정(창업자).
3. **보통/낮음 17건**: §1대로 반송. 특히 false-green 3건(M23·H7·H6 POD 멱등 테스트)은 테스트를 실경로 red-provable로 교체.
4. 수정은 웨이브 커밋 위에 fix 커밋으로 쌓고, 재감사는 동일 방식(게이트 재실행 + 실경로 red 확인)으로.