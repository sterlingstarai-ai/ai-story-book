# FastAPI
fastapi==0.115.0
uvicorn[standard]==0.32.0
# CVE-2024-53981(GHSA-59g5-xgcq-4qw3, <0.0.18): 악의적 multipart boundary 파싱이 CPU를
# 과소모해 async 이벤트루프를 블록 — 업로드 3개 엔드포인트가 near-unauth 도달 가능.
python-multipart==0.0.20

# Database
sqlalchemy==2.0.36
greenlet>=3.1.1,<4
alembic==1.14.0
psycopg2-binary>=2.9.9
asyncpg==0.31.0

# Celery
celery[redis]==5.4.0
redis==5.2.0

# Pydantic
pydantic==2.10.0
pydantic-settings==2.6.0

# S3
boto3==1.35.0

# HTTP Client
httpx==0.28.1
# aiohttp 제거(CI-1): src/·tests/ 어디서도 import하지 않는 고아 의존이었고(역의존 0 실측),
# 상향해도 수정본 없는 CVE가 남았다. 안 쓰는 의존을 지워 현재 CVE + 향후 aiohttp CVE를
# 영구 제거하고 공격 표면·이미지 크기도 줄인다. HTTP 클라이언트 정본은 httpx.
google-auth==2.40.0

# Templating
# CVE 수정본 존재(<3.1.6 취약: 샌드박스 우회 계열 3건) — 실제 상향 가능해 즉시 적용.
jinja2==3.1.6

# Utils
python-dotenv==1.2.1
structlog==24.4.0

# PDF Generation
reportlab==4.2.5

# Testing
pytest==8.3.0
pytest-asyncio==0.24.0
pytest-cov==6.0.0
aiosqlite==0.20.0
